{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#arkitekt","title":"Arkitekt","text":"<p>An Android and Kotlin Multiplatform architecture library built on Jetpack Compose and Kotlin Coroutines.</p> <p>Arkitekt is a testable foundation for Android and KMP apps. It offers two architecture paths that share the same core principles:</p> <ul> <li>Android / Compose: ViewModel + Hilt + Jetpack Compose</li> <li>Decompose / KMP: BaseComponent + Koin + Kotlin Multiplatform</li> </ul>"},{"location":"#key-features","title":"Key features","text":"<ul> <li>Compose-ready ViewModels with built-in state management and one-shot events</li> <li>Coroutine use cases for testable business logic (<code>UseCase</code> and <code>FlowUseCase</code>)</li> <li>Kotlin Multiplatform support via Decompose with Koin dependency injection</li> <li>A KSP factory generator for Decompose components (<code>@GenerateFactory</code>)</li> <li><code>kotlin.Result</code> integration with <code>getOrCancel</code> for safe coroutine error handling</li> <li>Testing utilities for mocking use cases, testing ViewModels, and testing Decompose components</li> </ul>"},{"location":"#quick-installation","title":"Quick installation","text":"<pre><code>dependencies {\n    // Android / Compose\n    implementation(\"app.futured.arkitekt:core:6.1.0\")\n    implementation(\"app.futured.arkitekt:cr-usecases:6.1.0\")\n    implementation(\"app.futured.arkitekt:compose:6.1.0\")\n\n    // Decompose / KMP (optional)\n    implementation(\"app.futured.arkitekt:decompose:6.1.0\")\n    implementation(\"app.futured.arkitekt:decompose-annotation:6.1.0\")\n    ksp(\"app.futured.arkitekt:decompose-processor:6.1.0\")\n}\n</code></pre> <p>See Installation for full details and snapshot builds.</p>"},{"location":"#get-started","title":"Get started","text":""},{"location":"#android-compose","title":"Android / Compose","text":"<p>Set up a Jetpack Compose project with ViewModel, ViewState, and Hilt.</p> <p>Project Setup Quick Start</p>"},{"location":"#decompose-kmp","title":"Decompose / KMP","text":"<p>Set up a Kotlin Multiplatform project with BaseComponent, Koin, and Decompose.</p> <p>Project Setup Quick Start</p>"},{"location":"#sample-projects","title":"Sample projects","text":"<p>The sample projects below use the same patterns as this documentation.</p>"},{"location":"#android-compose_1","title":"Android / Compose","text":"<p>The <code>example</code> module in this repository contains a minimal Android sample. For a complete project setup, see the Android Project Template.</p>"},{"location":"#decompose-kmp_1","title":"Decompose / KMP","text":"<p>The KMP Futured Template is a full Kotlin Multiplatform project (Android + iOS) using BaseComponent, Koin, Decompose navigation, and KSP factory generation.</p>"},{"location":"lint/","title":"Lint","text":""},{"location":"lint/#lint-rules","title":"Lint rules","text":"<p>Arkitekt includes custom lint checks in the <code>arkitekt-lint</code> module to enforce naming conventions for event classes.</p>"},{"location":"lint/#rules","title":"Rules","text":""},{"location":"lint/#mvvmeventnamemissingsuffix","title":"MvvmEventNameMissingSuffix","text":"<p>Severity: Warning</p> <p>Event class names should end with the <code>Event</code> suffix. A quickfix is provided to rename the class.</p> <pre><code>// Warning: missing Event suffix\nclass ShowDetail : Event&lt;ViewState&gt;()\n\n// Correct\nclass ShowDetailEvent : Event&lt;ViewState&gt;()\n</code></pre>"},{"location":"lint/#mvvmeventnamemisspell","title":"MvvmEventNameMisspell","text":"<p>Severity: Warning</p> <p>Detects misspelled event names where extra characters appear after <code>Event</code>. A quickfix is provided to correct the name.</p> <pre><code>// Warning: extra characters after Event\nclass ShowDetailEvents : Event&lt;ViewState&gt;()\n\n// Correct\nclass ShowDetailEvent : Event&lt;ViewState&gt;()\n</code></pre>"},{"location":"lint/#scope","title":"Scope","text":"<p>These lint rules apply to all classes extending <code>Event&lt;T&gt;</code> from the Arkitekt <code>core</code> module. The lint checks are included automatically when you depend on the <code>core</code> module, so no extra configuration is needed.</p>"},{"location":"architecture/android/","title":"Android","text":""},{"location":"architecture/android/#android-architecture","title":"Android architecture","text":""},{"location":"architecture/android/#components","title":"Components","text":"<p>Arkitekt provides two ViewModel base classes for the Android / Compose path.</p>"},{"location":"architecture/android/#basecoreviewmodel","title":"BaseCoreViewModel","text":"<p><code>BaseCoreViewModel&lt;VS : ViewState&gt;</code> from the core module is an abstract <code>ViewModel</code> that gives you:</p> <ul> <li><code>viewState: VS</code>: the screen's view state instance</li> <li><code>events: Flow&lt;Event&lt;VS&gt;&gt;</code>: a channel-backed flow of one-shot events</li> <li><code>sendEvent(event)</code>: sends an event to the UI</li> </ul> <p>Use this when you do not need to execute use cases.</p> <pre><code>class SimpleViewModel(\n    override val viewState: SimpleViewState,\n) : BaseCoreViewModel&lt;SimpleViewState&gt;()\n</code></pre>"},{"location":"architecture/android/#baseviewmodel","title":"BaseViewModel","text":"<p><code>BaseViewModel&lt;VS : ViewState&gt;</code> from the compose module extends <code>BaseCoreViewModel</code> and implements <code>CoroutineScopeOwner</code> (from <code>cr-usecases</code>). Use this when you need to execute use cases.</p> <pre><code>@HiltViewModel\nclass HomeViewModel @Inject constructor(\n    override val viewState: HomeViewState,\n    private val getProfileUseCase: GetProfileUseCase,\n) : BaseViewModel&lt;HomeViewState&gt;() {\n\n    init {\n        getProfileUseCase.execute(Unit) {\n            onSuccess { viewState.userName = it.name }\n            onError { sendEvent(ShowErrorEvent(it.message)) }\n        }\n    }\n}\n</code></pre>"},{"location":"architecture/android/#coroutinescopeowner","title":"CoroutineScopeOwner","text":"<p><code>CoroutineScopeOwner</code> (from <code>cr-usecases</code>) is implemented by <code>BaseViewModel</code> and provides:</p> <ul> <li><code>useCaseScope</code>: the scope for executing use cases, backed by <code>viewModelScope</code></li> <li><code>getWorkerDispatcher()</code>: returns <code>Dispatchers.IO</code> by default; override for testing</li> <li><code>launchWithHandler {}</code>: launches a coroutine with try-catch that calls <code>defaultErrorHandler</code> and logs to <code>UseCaseErrorHandler.globalOnErrorLogger</code></li> <li><code>defaultErrorHandler(exception)</code>: by default rethrows the exception; override to customize error handling</li> </ul>"},{"location":"architecture/android/#obtaining-the-viewmodel-in-compose","title":"Obtaining the ViewModel in Compose","text":"<pre><code>@Composable\nfun HomeScreen(\n    viewModel: HomeViewModel = hiltViewModel(),\n) {\n    val title = viewModel.viewState.title\n    // ...\n}\n</code></pre>"},{"location":"architecture/android/#state-management","title":"State management","text":""},{"location":"architecture/android/#viewstate","title":"ViewState","text":"<p><code>ViewState</code> is a marker interface. Implement it with Compose <code>mutableStateOf</code> fields so the UI recomposes automatically when values change.</p> <pre><code>class HomeViewState @Inject constructor() : ViewState {\n    var title by mutableStateOf(\"\")\n    var isLoading by mutableStateOf(false)\n}\n</code></pre> <p><code>ViewState</code> is injected into the ViewModel and acts as the single source of truth for the screen's state.</p> <pre><code>@HiltViewModel\nclass HomeViewModel @Inject constructor(\n    override val viewState: HomeViewState,\n) : BaseViewModel&lt;HomeViewState&gt;() {\n\n    fun onNameLoaded(name: String) {\n        viewState.title = name\n    }\n}\n</code></pre> <p>You can also use <code>derivedStateOf</code> for computed properties:</p> <pre><code>class FormViewState @Inject constructor() : ViewState {\n    var login by mutableStateOf(\"\")\n    var password by mutableStateOf(\"\")\n    val isFormValid by derivedStateOf {\n        login.isNotBlank() &amp;&amp; password.isNotBlank()\n    }\n}\n</code></pre>"},{"location":"architecture/android/#observing-state-in-compose","title":"Observing state in Compose","text":"<p>Use Compose state delegation to observe ViewState fields:</p> <pre><code>@Composable\nfun HomeScreen(viewModel: HomeViewModel = hiltViewModel()) {\n    val title = viewModel.viewState.title\n    val isLoading = viewModel.viewState.isLoading\n\n    if (isLoading) {\n        CircularProgressIndicator()\n    } else {\n        Text(title)\n    }\n}\n</code></pre>"},{"location":"architecture/android/#stateflow-alternative","title":"StateFlow alternative","text":"<p>If you prefer <code>StateFlow</code> over Compose <code>mutableStateOf</code>, you can expose a <code>StateFlow</code> from the ViewModel and collect it in the Composable with <code>collectAsState()</code>:</p> <pre><code>class HomeViewState : ViewState {\n    val title = MutableStateFlow(\"\")\n}\n\n// In Composable\nval title by viewModel.viewState.title.collectAsState()\n</code></pre>"},{"location":"architecture/android/#events","title":"Events","text":"<p>Events are one-shot messages sent from a ViewModel to a Composable. They are backed by a <code>Channel</code>, which guarantees single delivery even during screen rotation.</p>"},{"location":"architecture/android/#defining-events","title":"Defining events","text":"<p>Define events as a sealed class extending <code>Event&lt;VS&gt;</code>:</p> <pre><code>sealed class HomeEvent : Event&lt;HomeViewState&gt;() {\n    object ShowDetailEvent : HomeEvent()\n    data class ShowMessageEvent(val message: String) : HomeEvent()\n}\n</code></pre>"},{"location":"architecture/android/#sending-events","title":"Sending events","text":"<p>Send an event from the ViewModel:</p> <pre><code>sendEvent(ShowDetailEvent)\n</code></pre>"},{"location":"architecture/android/#collecting-events-in-compose","title":"Collecting events in Compose","text":"<p>Use <code>EventsEffect</code> to collect events in a Composable:</p> <pre><code>@Composable\nfun HomeScreen(viewModel: HomeViewModel = hiltViewModel()) {\n    viewModel.EventsEffect {\n        onEvent&lt;ShowDetailEvent&gt; { /* navigate to detail */ }\n        onEvent&lt;ShowMessageEvent&gt; { showSnackbar(it.message) }\n    }\n}\n</code></pre> <p><code>EventsEffect</code> is an extension function on <code>BaseCoreViewModel</code> from the compose module. It launches a coroutine that collects events for the lifetime of the Composable.</p> <p><code>onEvent&lt;E&gt;</code> is a type-safe filter: it checks whether the received event is of type <code>E</code> and executes the lambda only when it matches.</p>"},{"location":"architecture/android/#navigation","title":"Navigation","text":"<p>Arkitekt does not provide its own navigation library for the Android path. Use Jetpack Navigation with Compose or any other navigation solution you prefer.</p>"},{"location":"architecture/android/#example-with-jetpack-navigation","title":"Example with Jetpack Navigation","text":"<pre><code>@Composable\nfun AppNavHost(navController: NavHostController = rememberNavController()) {\n    NavHost(navController = navController, startDestination = \"home\") {\n        composable(\"home\") {\n            HomeScreen(onNavigateToDetail = { navController.navigate(\"detail/$it\") })\n        }\n        composable(\"detail/{id}\") { backStackEntry -&gt;\n            DetailScreen(id = backStackEntry.arguments?.getString(\"id\").orEmpty())\n        }\n    }\n}\n</code></pre> <p>For a working implementation, see the example module in the Arkitekt GitHub repository.</p>"},{"location":"architecture/overview/","title":"Overview","text":""},{"location":"architecture/overview/#overview","title":"Overview","text":"<p>Arkitekt provides a set of architectural patterns for building modern apps. The same core ideas apply whether you are targeting Android only or building a Kotlin Multiplatform project.</p>"},{"location":"architecture/overview/#two-paths-one-approach","title":"Two paths, one approach","text":"<ul> <li>Android / Compose: <code>ViewModel</code> + <code>ViewState</code> + Hilt DI</li> <li>KMP / Decompose: <code>BaseComponent</code> + State (data class) + Koin DI</li> </ul> <p>Both paths share:</p> <ul> <li>Use cases for business logic (<code>UseCase</code>, <code>FlowUseCase</code> from <code>cr-usecases</code>)</li> <li>Stores (repositories) for data access and caching</li> <li>Events for one-shot UI communication</li> </ul>"},{"location":"architecture/overview/#architecture-layers","title":"Architecture layers","text":"Layer Android / Compose KMP / Decompose UI <code>@Composable</code> <code>@Composable</code> Presentation <code>ViewModel</code> <code>BaseComponent</code> Business Logic <code>UseCase</code> / <code>FlowUseCase</code> <code>UseCase</code> / <code>FlowUseCase</code> Data <code>Store</code> / <code>Dao</code> / <code>ApiService</code> <code>Store</code> / <code>Dao</code> / <code>ApiService</code>"},{"location":"architecture/overview/#when-to-choose-which-path","title":"When to choose which path","text":"<ul> <li>Android-only projects: use the Android path (<code>core</code> + <code>compose</code> modules with Hilt).</li> <li>KMP projects targeting Android and iOS: use the Decompose path (<code>decompose</code> module with Koin).</li> </ul> <p>Note</p> <p>The KMP / Decompose path supports Android and iOS targets. Desktop and Web targets are not supported.</p> <p>Both paths depend on the <code>cr-usecases</code> module, which is fully KMP-compatible. <code>UseCase</code> and <code>FlowUseCase</code> are interfaces, so business logic is shared across all targets regardless of the path you choose. Execution is driven through <code>CoroutineScopeOwner</code> via Kotlin context parameter extensions.</p>"},{"location":"architecture/overview/#related-pages","title":"Related pages","text":"<ul> <li>Android Architecture: components, state, events, navigation</li> <li>KMP Architecture: components, state, events, navigation, factory generator</li> <li>Stores: the shared data layer pattern</li> </ul>"},{"location":"architecture/stores/","title":"Stores","text":""},{"location":"architecture/stores/#stores-repositories","title":"Stores (repositories)","text":"<p>Stores follow the repository pattern. Each store handles a single entity's data logic.</p> <p>Examples: <code>UserStore</code>, <code>OrderStore</code>, <code>DeviceStore</code>.</p>"},{"location":"architecture/stores/#defining-a-store","title":"Defining a store","text":"<pre><code>@Singleton\nclass UserStore @Inject constructor() {\n    private val userState = MutableStateFlow(User.EMPTY)\n\n    fun setUser(user: User) {\n        userState.value = user\n    }\n\n    fun getUser(): StateFlow&lt;User&gt; = userState\n}\n</code></pre>"},{"location":"architecture/stores/#data-layer-role","title":"Data layer role","text":"<p>Stores are injected into use cases, not into ViewModels or Components directly. Besides custom stores, Room <code>Dao</code>s and Retrofit/Ktor API interfaces serve the same role in the data layer.</p>"},{"location":"architecture/stores/#wrapping-store-access-with-a-use-case","title":"Wrapping store access with a use case","text":"<pre><code>class ObserveUserFullNameUseCase @Inject constructor(\n    private val userStore: UserStore,\n) : FlowUseCase&lt;Unit, String&gt; {\n    override fun build(args: Unit): Flow&lt;String&gt; =\n        userStore.getUser().map { \"${it.firstName} ${it.lastName}\" }\n}\n</code></pre> <p>This pattern is the same for both the Android and KMP paths.</p>"},{"location":"architecture/kmp/components/","title":"Components","text":""},{"location":"architecture/kmp/components/#components-kmp","title":"Components (KMP)","text":"<p>The <code>decompose</code> module provides the building blocks for KMP presentation logic, built on top of the Decompose library.</p>"},{"location":"architecture/kmp/components/#basecomponent","title":"BaseComponent","text":"<p><code>BaseComponent&lt;VS : Any, E : Any&gt;</code> is the main base class for components, provided by the Arkitekt library.</p> <p>Constructor parameters:</p> <ul> <li><code>componentContext: GenericComponentContext&lt;*&gt;</code> is the Decompose component context</li> <li><code>defaultState: VS</code> is the initial state value</li> <li><code>lifecycleScope: CoroutineScope</code> is the scope tied to the component lifecycle (defaults to <code>MainScope()</code>; inject your own scope in tests)</li> </ul> <p>Key members:</p> <ul> <li><code>componentState: MutableStateFlow&lt;VS&gt;</code> is the protected mutable state</li> <li><code>lifecycleScope: CoroutineScope</code> is public and open; it is cancelled automatically when the component is destroyed</li> <li><code>events: Flow&lt;E&gt;</code> is a single-subscriber flow of one-shot UI events backed by a buffered <code>Channel</code></li> <li><code>sendUiEvent(event: E)</code> is a protected function to emit an event</li> <li><code>fun Flow&lt;VS&gt;.asStateFlow(started): StateFlow&lt;VS&gt;</code> is a protected helper to convert a <code>Flow</code> to a <code>StateFlow</code> within the component scope</li> </ul>"},{"location":"architecture/kmp/components/#events-semantics","title":"Events semantics","text":"<p><code>events</code> is backed by a <code>Channel.BUFFERED</code> (capacity 64). Events emitted before a collector subscribes are buffered and delivered when the collector starts. Because it is a channel-based flow, each event is delivered exactly once to a single collector. If multiple collectors subscribe, only one receives each event. On configuration change (collector cancelled and restarted), any undelivered buffered events are drained to the new collector.</p>"},{"location":"architecture/kmp/components/#lifecycle","title":"Lifecycle","text":"<p>The component lifecycle is driven by Decompose's <code>LifecycleOwner</code>. The recommended place to start work is inside a <code>doOnStart</code> / <code>doOnCreate</code> block:</p> <pre><code>init {\n    lifecycle.doOnStart {\n        observeUserUseCase.execute(Unit) {\n            onNext { update(componentState) { copy(userName = it.name) } }\n        }\n    }\n}\n</code></pre> <p>When the lifecycle is destroyed, <code>lifecycleScope</code> is cancelled and the <code>events</code> channel is closed. Any subsequent <code>sendUiEvent</code> calls are no-ops.</p>"},{"location":"architecture/kmp/components/#arkitektcomponentcontext","title":"ArkitektComponentContext","text":"<p><code>ArkitektComponentContext&lt;T&gt;</code> is a marker interface extending Decompose's <code>GenericComponentContext&lt;T&gt;</code>. It serves as the base for your project's own component context type.</p> <p>The recommended pattern is to define an <code>AppComponentContext</code> interface in your project that self-references the type parameter, and a <code>DefaultAppComponentContext</code> implementation that delegates to a standard Decompose <code>ComponentContext</code>:</p> <pre><code>// commonMain: define once per project\ninterface AppComponentContext : ArkitektComponentContext&lt;AppComponentContext&gt;\n\nclass DefaultAppComponentContext(componentContext: ComponentContext) :\n    AppComponentContext,\n    LifecycleOwner by componentContext,\n    StateKeeperOwner by componentContext,\n    InstanceKeeperOwner by componentContext,\n    BackHandlerOwner by componentContext {\n\n    override val componentContextFactory: ComponentContextFactory&lt;AppComponentContext&gt; =\n        ComponentContextFactory { lifecycle, stateKeeper, instanceKeeper, backHandler -&gt;\n            val ctx = componentContext.componentContextFactory(lifecycle, stateKeeper, instanceKeeper, backHandler)\n            DefaultAppComponentContext(ctx)\n        }\n}\n</code></pre>"},{"location":"architecture/kmp/components/#creating-the-root-component","title":"Creating the root component","text":"<p>On Android, Arkitekt is designed to be used with Decompose's <code>retainedComponent</code> to create the root component. This retains the entire component tree across configuration changes, similar to AndroidX <code>ViewModel</code>:</p> <pre><code>// Android Activity, onCreate\nval rootComponent = retainedComponent { componentContext -&gt;\n    RootNavHostComponent(DefaultAppComponentContext(componentContext))\n}\n</code></pre> <p>Note</p> <p><code>retainedComponent</code> is an Android-specific extension on <code>ComponentActivity</code>. It should be called once in <code>onCreate</code>. On iOS, create a <code>DefaultComponentContext</code> with the application lifecycle and pass it directly.</p> <p>Child components always receive their <code>AppComponentContext</code> from the parent; they never construct it themselves. Decompose creates a scoped child context automatically when you call <code>childStack</code>, <code>childSlot</code>, or similar APIs.</p>"},{"location":"architecture/kmp/components/#durable-navigation-results","title":"Durable navigation results","text":"<p>To pass results between destinations that survive configuration changes and process death (see Advanced navigation \u2192 Passing results between screens), expose a <code>NavigationResultRegistry</code> on your context. By default <code>ArkitektComponentContext.navigationResultRegistry</code> throws, so this is opt-in.</p> <p>Construct a single registry at the root over the root component's <code>StateKeeper</code> (the one restored after process death), and return that same instance from every child context the factory creates:</p> <pre><code>class DefaultAppComponentContext private constructor(\n    componentContext: ComponentContext,\n    override val navigationResultRegistry: NavigationResultRegistry,\n) : AppComponentContext,\n    LifecycleOwner by componentContext,\n    StateKeeperOwner by componentContext,\n    InstanceKeeperOwner by componentContext,\n    BackHandlerOwner by componentContext {\n\n    // Root entry point: create the registry once over the root StateKeeper.\n    constructor(componentContext: ComponentContext) : this(\n        componentContext = componentContext,\n        navigationResultRegistry = NavigationResultRegistry(componentContext.stateKeeper),\n    )\n\n    override val componentContextFactory: ComponentContextFactory&lt;AppComponentContext&gt; =\n        ComponentContextFactory { lifecycle, stateKeeper, instanceKeeper, backHandler -&gt;\n            val ctx = componentContext.componentContextFactory(lifecycle, stateKeeper, instanceKeeper, backHandler)\n            // Share the SAME registry instance down the tree.\n            DefaultAppComponentContext(ctx, navigationResultRegistry)\n        }\n}\n</code></pre> <p>Because the registry rides on the root <code>StateKeeper</code>, undelivered results are saved on process death and replayed when the parent re-attaches its collector. The <code>NavigationResultRegistry(stateKeeper)</code> factory returns the framework-provided implementation.</p>"},{"location":"architecture/kmp/components/#appcomponent","title":"AppComponent","text":"<p>It is recommended to define an application-level base class that extends <code>BaseComponent</code> and delegates <code>AppComponentContext</code>:</p> <pre><code>abstract class AppComponent&lt;VS : Any, E : Any&gt;(\n    componentContext: AppComponentContext,\n    defaultState: VS,\n) : BaseComponent&lt;VS, E&gt;(componentContext, defaultState),\n    AppComponentContext by componentContext\n</code></pre> <p>Because <code>AppComponent</code> delegates <code>AppComponentContext</code>, all context services (<code>lifecycle</code>, <code>stateKeeper</code>, <code>instanceKeeper</code>, <code>backHandler</code>) are directly accessible on every component without going through <code>componentContext</code>. This is the recommended approach for both screen components and nav-host components.</p> <p>This pattern also makes it easy to integrate with use cases: implement <code>CoroutineScopeOwner</code> directly on <code>AppComponent</code> to make use case execution available in every component by default. See Use Cases.</p>"},{"location":"architecture/kmp/components/#koin-factory-generation","title":"Koin factory generation","text":"<p>Use the <code>@GenerateFactory</code> annotation to generate Koin dependency injection factories for your components. See the Factory Generator page for full details and KSP configuration.</p>"},{"location":"architecture/kmp/components/#complete-example","title":"Complete example","text":"<pre><code>@GenerateFactory\nclass HomeComponent(\n    @InjectedParam componentContext: AppComponentContext,\n    @InjectedParam navigation: HomeNavigation,\n    private val observeUserUseCase: ObserveUserUseCase,\n) : AppComponent&lt;HomeState, HomeUiEvent&gt;(componentContext, HomeState()),\n    CoroutineScopeOwner {\n\n    override val useCaseScope = lifecycleScope\n    override val useCaseJobPool = mutableMapOf&lt;Any, Job&gt;()\n\n    val state: StateFlow&lt;HomeState&gt; = componentState\n\n    init {\n        lifecycle.doOnStart {\n            observeUserUseCase.execute(Unit) {\n                onNext { update(componentState) { copy(userName = it.name) } }\n                onError { sendUiEvent(HomeUiEvent.ShowError(it.message.orEmpty())) }\n            }\n        }\n    }\n\n    fun onDetailClick() {\n        navigation.toDetail()\n    }\n}\n</code></pre> <p>Note</p> <p><code>lifecycle.doOnStart</code> is the recommended place to trigger work that should run each time the component becomes active. The <code>lifecycle</code> property is available implicitly because <code>AppComponent</code> delegates <code>AppComponentContext</code>. This also makes components easy to unit test: you can control the lifecycle externally and verify behavior at each stage.</p> <p>Note</p> <p><code>BaseComponent</code> does not implement <code>CoroutineScopeOwner</code> directly. To execute use cases, implement <code>CoroutineScopeOwner</code> in your component and set <code>useCaseScope = lifecycleScope</code>.</p>"},{"location":"architecture/kmp/components/#example-application","title":"Example application","text":"<p>For a full KMP application that uses the patterns described in this documentation, see the KMP Futured Template repository.</p>"},{"location":"architecture/kmp/events/","title":"Events","text":""},{"location":"architecture/kmp/events/#events-kmp","title":"Events (KMP)","text":"<p>Events are one-shot messages sent from a Component to the UI layer.</p>"},{"location":"architecture/kmp/events/#defining-events","title":"Defining events","text":"<p>Define events as a sealed interface extending <code>UiEvent</code>:</p> <pre><code>sealed interface HomeUiEvent : UiEvent {\n    data object ShowToast : HomeUiEvent\n    data class ShowError(val message: String) : HomeUiEvent\n}\n</code></pre>"},{"location":"architecture/kmp/events/#sending-events","title":"Sending events","text":"<p>Send an event from the Component:</p> <pre><code>sendUiEvent(HomeUiEvent.ShowToast)\n</code></pre>"},{"location":"architecture/kmp/events/#collecting-events-in-compose","title":"Collecting events in Compose","text":"<p>Use <code>EventsEffect</code> from the decompose module:</p> <pre><code>@Composable\nfun HomeScreen(component: HomeComponent) {\n    EventsEffect(component.events) {\n        onEvent&lt;HomeUiEvent.ShowToast&gt; { /* show toast */ }\n        onEvent&lt;HomeUiEvent.ShowError&gt; { showSnackbar(it.message) }\n    }\n}\n</code></pre> <p>Note</p> <p>Unlike the Android path where <code>EventsEffect</code> is an extension on <code>BaseCoreViewModel</code>, the KMP variant takes the <code>eventsFlow</code> parameter directly (e.g. <code>component.events</code>).</p>"},{"location":"architecture/kmp/factory-generator/","title":"Factory Generator","text":""},{"location":"architecture/kmp/factory-generator/#factory-generator-koin","title":"Factory generator (Koin)","text":"<p>The <code>@GenerateFactory</code> annotation triggers KSP code generation to create Koin dependency injection factory objects for Decompose components. This eliminates the need to manually wire Koin dependencies when creating components in navigation factories.</p>"},{"location":"architecture/kmp/factory-generator/#usage","title":"Usage","text":"<p>Annotate your component class with <code>@GenerateFactory</code>. Mark parameters that should be passed directly to the factory with <code>@InjectedParam</code>. All other constructor parameters are resolved from Koin automatically.</p> <pre><code>@GenerateFactory\nclass HomeComponent(\n    @InjectedParam componentContext: AppComponentContext,\n    @InjectedParam navigation: HomeNavigation,\n    private val someUseCase: SomeUseCase, // resolved by Koin\n) : BaseComponent&lt;HomeState, HomeUiEvent&gt;(componentContext, HomeState())\n</code></pre>"},{"location":"architecture/kmp/factory-generator/#generated-output","title":"Generated output","text":"<p>The annotation processor generates an <code>internal object</code> factory that implements <code>KoinComponent</code>:</p> <pre><code>internal object HomeComponentFactory : KoinComponent {\n    fun createComponent(\n        componentContext: AppComponentContext,\n        navigation: HomeNavigation,\n    ): HomeComponent = get(\n        qualifier = null,\n        parameters = { parametersOf(componentContext, navigation) },\n    )\n}\n</code></pre> <ul> <li>Parameters marked with <code>@InjectedParam</code> become <code>createComponent()</code> parameters and are forwarded to Koin via <code>parametersOf</code></li> <li>The component itself is resolved from Koin, so it must be registered in your Koin module (typically as a <code>factory</code>)</li> <li>All non-<code>@InjectedParam</code> constructor dependencies (e.g. <code>SomeUseCase</code>) are resolved by Koin from the DI graph</li> </ul> <pre><code>// Koin module\nval homeModule = module {\n    factory { params -&gt;\n        HomeComponent(\n            componentContext = params.get(),\n            navigation = params.get(),\n            someUseCase = get(),\n        )\n    }\n}\n</code></pre>"},{"location":"architecture/kmp/factory-generator/#calling-the-factory","title":"Calling the factory","text":"<p>Use the generated factory inside a nav-host's <code>childStack</code> child factory:</p> <pre><code>val stack = childStack(\n    source = navigation.stackNavigator,\n    serializer = AppDestination.serializer(),\n    initialConfiguration = AppDestination.Home,\n    childFactory = { destination, ctx -&gt;\n        when (destination) {\n            AppDestination.Home -&gt; HomeComponentFactory.createComponent(ctx, navigation)\n            AppDestination.Detail -&gt; DetailComponentFactory.createComponent(ctx, navigation)\n        }\n    },\n).asStateFlow()\n</code></pre> <p><code>ctx</code> is the child <code>AppComponentContext</code> provided by Decompose; <code>navigation</code> is the nav-host's navigation instance passed through as an <code>@InjectedParam</code>.</p>"},{"location":"architecture/kmp/factory-generator/#ksp-configuration-for-kmp","title":"KSP configuration for KMP","text":"<p>Important</p> <p>Use <code>kspCommonMainMetadata</code> only. Do not add the processor to platform-specific configurations (<code>kspAndroid</code>, <code>kspIosArm64</code>, etc.) as this would cause duplicate generation.</p> <p>Add the following to your module's <code>build.gradle.kts</code>:</p> <pre><code>plugins {\n    id(\"com.google.devtools.ksp\")\n}\n\ndependencies {\n    add(\"kspCommonMainMetadata\", \"app.futured.arkitekt:decompose-processor:6.1.0\")\n}\n\nkotlin.sourceSets.named(\"commonMain\") {\n    kotlin.srcDir(\"build/generated/ksp/metadata/commonMain/kotlin\")\n}\n\ntasks.withType&lt;org.jetbrains.kotlin.gradle.tasks.KotlinCompilationTask&lt;*&gt;&gt;().configureEach {\n    if (name != \"kspCommonMainKotlinMetadata\") {\n        dependsOn(\"kspCommonMainKotlinMetadata\")\n    }\n}\n</code></pre> <p>This configuration ensures KSP runs on common metadata and the generated sources are available to all platform targets.</p>"},{"location":"architecture/kmp/navigation-advanced/","title":"Advanced navigation","text":""},{"location":"architecture/kmp/navigation-advanced/#advanced-navigation","title":"Advanced navigation","text":"<p>This page covers advanced navigation patterns for the KMP / Decompose path. For the basics, see Navigation (KMP).</p>"},{"location":"architecture/kmp/navigation-advanced/#consolidated-navigation","title":"Consolidated navigation","text":"<p>For nav-hosts with many screens, we recommend consolidating all navigation logic into a dedicated class. This keeps the nav-host component decluttered and navigation logic easy to find.</p> <p>The pattern works as follows:</p> <ol> <li>Each screen component defines its navigation as an interface with extension functions on itself (e.g. <code>fun HomeComponent.navigateToDetail()</code>). This scopes each navigation call to the component that triggers it and allows duplicate function names like <code>navigateBack()</code> across different screens.</li> <li>The nav-host defines an internal interface that extends all child screen navigation interfaces and holds the <code>stackNavigator</code>.</li> <li>A single implementation class provides all navigation logic.</li> </ol> <pre><code>// Each screen defines its own navigation interface\ninterface HomeScreenNavigation : NavigationActions {\n    fun HomeComponent.navigateToDetail()\n}\n\ninterface DetailScreenNavigation : NavigationActions {\n    fun DetailComponent.navigateBack()\n}\n\n// The nav-host's internal interface consolidates them all\ninternal interface HomeNavHostNavigation :\n    HomeScreenNavigation,\n    DetailScreenNavigation {\n    val stackNavigator: StackNavigation&lt;HomeDestination&gt;\n}\n\n// Single implementation handles all navigation\ninternal class HomeNavHostNavigationImpl : HomeNavHostNavigation {\n    override val stackNavigator = StackNavigation&lt;HomeDestination&gt;()\n\n    override fun HomeComponent.navigateToDetail() =\n        stackNavigator.pushNew(HomeDestination.Detail)\n\n    override fun DetailComponent.navigateBack() =\n        stackNavigator.pop()\n}\n</code></pre> <p>The nav-host receives the navigation instance and passes it to child factories. Because the consolidated interface extends every screen's navigation interface, the same instance satisfies all children:</p> <pre><code>@GenerateFactory\ninternal class HomeNavHostComponent(\n    @InjectedParam componentContext: AppComponentContext,\n    @InjectedParam private val navigation: HomeNavHostNavigation,\n) : AppComponent&lt;Unit, Nothing&gt;(componentContext, Unit) {\n\n    val stack = childStack(\n        source = navigation.stackNavigator,\n        serializer = HomeDestination.serializer(),\n        initialConfiguration = HomeDestination.Home,\n        childFactory = { destination, ctx -&gt;\n            when (destination) {\n                HomeDestination.Home -&gt; HomeComponentFactory.createComponent(ctx, navigation)\n                HomeDestination.Detail -&gt; DetailComponentFactory.createComponent(ctx, navigation)\n            }\n        },\n    ).asStateFlow()\n}\n</code></pre> <p>Tip</p> <p>This pattern is not enforced by the library. For simple nav-hosts with one or two screens, inline anonymous objects (as shown in the Parent component section) work fine. The consolidated approach pays off as the number of screens and cross-screen navigation grows.</p>"},{"location":"architecture/kmp/navigation-advanced/#passing-results-between-screens","title":"Passing results between screens","text":"<p>Use <code>ResultFlow&lt;T&gt;</code> to send a value from a child screen back to its parent. Results are routed by a stable key declared as a typed <code>ResultKey&lt;T&gt;</code>: the parent collects results for a key, the child sends a result for the same key, and the navigation config carries the key itself (which serializes down to just its name), never a live object.</p> <p>Results survive configuration changes and process death. Each value is persisted into saved state until it is collected once, so a result the child produced before the OS killed the process still reaches the parent after it is recreated. This needs a one-time integration step: wiring the result registry into your <code>AppComponentContext</code>, described in Components \u2192 Durable navigation results.</p> <p>Note</p> <p>Results must be <code>@Serializable</code>, one-shot, and small. They ride in the Android saved-state <code>Bundle</code> (~1 MB limit), so use them for ids and selections, not large payloads.</p> <p>Declare keys as constants with <code>ResultKey&lt;T&gt;</code> so the type travels with the key and every key stays in one place. <code>ResultKey&lt;T&gt;</code> is itself <code>@Serializable</code>, so the typed key rides inside the child's config: the parent pushes it and the child reads it back fully typed, with no need to restate the type or re-wrap a raw string.</p> <pre><code>object HomeResultKeys {\n    val Picker = ResultKey&lt;String&gt;(\"home.picker\")\n}\n\n// Navigation config: carries the typed key (only its name is serialized)\n@Serializable\ndata class PickerConfig(val resultKey: ResultKey&lt;String&gt;)\n\n// In the parent nav-host\nprivate val pickerResult = resultFlow(HomeResultKeys.Picker)\n\ninit {\n    lifecycle.doOnCreate {\n        pickerResult\n            .onEach { selected -&gt; update(componentState) { copy(selection = selected) } }\n            .launchIn(lifecycleScope)\n    }\n}\n\nprivate fun openPicker() = stackNavigation.push(PickerConfig(HomeResultKeys.Picker))\n\n// In the child (picker) component, which received the typed resultKey from its config\nfun onItemSelected(item: String) = launchWithHandler {\n    resultFlow(resultKey).sendResult(item) // suspending; call from a coroutine\n    navigation.back()\n}\n</code></pre>"},{"location":"architecture/kmp/navigation-advanced/#key-uniqueness","title":"Key uniqueness","text":"<p>Keys are application-wide, so each parent must use a unique key; namespace them per destination (<code>\"home.picker\"</code>, not <code>\"picker\"</code>). Reusing the same child component across two navigation branches is safe: the child only echoes back whatever <code>resultKey</code> its config was given. If two parents use the same key while both are on the stack, collecting the second throws an <code>IllegalStateException</code> right away instead of silently crossing results between branches. Only one collector per key can be active at a time. For multiple concurrent instances of the same screen, build a per-instance key at push time and put it in the config (<code>ResultKey&lt;String&gt;(\"home.picker.$itemId\")</code>); only its name is serialized, and the type still travels with the field. For a static single-instance key, the parent and child can instead both reference the shared <code>HomeResultKeys.Picker</code> constant directly, with nothing in the config.</p>"},{"location":"architecture/kmp/navigation-advanced/#resultflow-api-reference","title":"ResultFlow API reference","text":"<p><code>ResultFlow&lt;T&gt;</code> extends <code>Flow&lt;T&gt;</code> and adds <code>suspend fun sendResult(item: T)</code> to send a value back from a child destination to a parent. Obtain a durable instance from a component with the <code>resultFlow</code> extension on the component context.</p>"},{"location":"architecture/kmp/navigation-advanced/#obtaining-a-resultflow","title":"Obtaining a ResultFlow","text":"<pre><code>// From a typed key constant\nval pickerResult = resultFlow(HomeResultKeys.Picker)\n\n// Or from a per-instance key built at push time\nval pickerResult = resultFlow(ResultKey&lt;String&gt;(\"home.picker.$itemId\"))\n</code></pre> <p>Sent values live in the application-wide <code>NavigationResultRegistry</code> (exposed on <code>ArkitektComponentContext</code>), which persists undelivered results into the root component's <code>StateKeeper</code>. Collect in <code>init { lifecycle.doOnCreate { ... } }</code>: every time the parent is recreated it re-attaches its collector and replays any result that arrived while it was gone.</p>"},{"location":"architecture/kmp/navigation/","title":"Navigation","text":""},{"location":"architecture/kmp/navigation/#navigation-kmp","title":"Navigation (KMP)","text":"<p>The KMP path uses the Decompose library for navigation. See the official Decompose navigation docs for the full API reference.</p> <p>For advanced patterns, see Advanced navigation.</p>"},{"location":"architecture/kmp/navigation/#navigationactions","title":"NavigationActions","text":"<p><code>NavigationActions</code> is a marker interface for defining navigation contracts:</p> <pre><code>interface HomeNavigation : NavigationActions {\n    fun toDetail()\n    fun toSettings()\n}\n</code></pre>"},{"location":"architecture/kmp/navigation/#navigationactionsproducer","title":"NavigationActionsProducer","text":"<p><code>NavigationActionsProducer&lt;NAV&gt;</code> is an interface for components that expose a navigation contract:</p> <pre><code>class HomeComponent(\n    componentContext: AppComponentContext,\n    private val navigation: HomeNavigation,\n) : BaseComponent&lt;HomeState, HomeUiEvent&gt;(componentContext, HomeState()),\n    NavigationActionsProducer&lt;HomeNavigation&gt; {\n\n    override val navigation = navigation\n\n    fun onDetailClick() {\n        navigation.toDetail()\n    }\n}\n</code></pre>"},{"location":"architecture/kmp/navigation/#parent-component","title":"Parent component","text":"<p>Navigation is typically implemented in the parent component that manages the child stack. The parent creates a <code>StackNavigation</code>, defines configurations, and implements the <code>NavigationActions</code> interface for each child:</p> <pre><code>class RootComponent(\n    componentContext: AppComponentContext,\n) : BaseComponent&lt;Unit, Nothing&gt;(componentContext, Unit) {\n\n    private val stackNavigation = StackNavigation&lt;Config&gt;()\n\n    val childStack = childStack(\n        source = stackNavigation,\n        initialConfiguration = Config.Home,\n        childFactory = ::createChild,\n    )\n\n    private fun createChild(config: Config, componentContext: ComponentContext) = when (config) {\n        Config.Home -&gt; HomeComponent(\n            componentContext = componentContext,\n            navigation = object : HomeNavigation {\n                override fun toDetail() { stackNavigation.push(Config.Detail) }\n                override fun toSettings() { stackNavigation.push(Config.Settings) }\n            },\n        )\n        // ...\n    }\n}\n</code></pre>"},{"location":"architecture/kmp/navigation/#switchtab","title":"switchTab","text":"<p><code>switchTab</code> is an extension on <code>StackNavigator&lt;C&gt;</code> designed for bottom navigation. It works like <code>bringToFront</code> but does not recreate the configuration if its class is already on the stack. It brings the existing instance to the front and preserves its state.</p> <pre><code>// In a nav-host managing bottom navigation tabs\nfun onTabSelected(tab: Tab) {\n    val config = when (tab) {\n        Tab.Home -&gt; Config.Home\n        Tab.Profile -&gt; Config.Profile\n    }\n    stackNavigation.switchTab(config)\n}\n</code></pre>"},{"location":"architecture/kmp/navigation/#navhost-pattern","title":"NavHost pattern","text":"<p>For apps with multiple sections (tabs, auth states), use a hierarchy of nav-host components. Each nav-host can own any combination of <code>StackNavigation</code> and <code>SlotNavigation</code> instances, for example a stack for the main flow and one or more slots for dialogs or bottom sheets. It implements the <code>NavigationActions</code> interfaces for the children it creates.</p> <p>Screen components define a <code>NavigationActions</code> interface but never implement it. The parent nav-host provides the implementation as an anonymous object in <code>createChild</code>. This keeps screen components fully decoupled from the navigation structure above them.</p> <p>A typical 3-level hierarchy looks like this:</p> <pre><code>RootNavHostComponent       - SlotNavigation: Login \u2194 SignedIn\n  SignedInNavHostComponent - StackNavigation: Home tab / Profile tab\n    HomeNavHostComponent   - StackNavigation: First \u2192 Second \u2192 Third\n</code></pre> <pre><code>// Top-level: switches between login and signed-in using a slot\nclass RootNavHostComponent(\n    ctx: AppComponentContext,\n) : BaseComponent&lt;Unit, Nothing&gt;(ctx, Unit) {\n\n    private val navigation = SlotNavigation&lt;RootConfig&gt;()\n\n    val child = childSlot(\n        source = navigation,\n        serializer = RootConfig.serializer(),\n        initialConfiguration = { RootConfig.Login },\n        childFactory = ::createChild,\n    )\n\n    private fun createChild(config: RootConfig, ctx: ComponentContext) = when (config) {\n        RootConfig.Login -&gt; LoginComponent(\n            componentContext = ctx.toAppContext(),\n            navigation = object : LoginNavigation {\n                override fun onLoginSuccess() = navigation.activate(RootConfig.SignedIn)\n            },\n        )\n        RootConfig.SignedIn -&gt; SignedInNavHostComponent(ctx.toAppContext())\n    }\n}\n\n// Second level: manages top-level tab stack\nclass SignedInNavHostComponent(\n    ctx: AppComponentContext,\n) : BaseComponent&lt;Unit, Nothing&gt;(ctx, Unit) {\n\n    private val stackNavigation = StackNavigation&lt;SignedInConfig&gt;()\n\n    val childStack = childStack(\n        source = stackNavigation,\n        serializer = SignedInConfig.serializer(),\n        initialConfiguration = SignedInConfig.Home,\n        childFactory = ::createChild,\n    )\n\n    private fun createChild(config: SignedInConfig, ctx: ComponentContext) = when (config) {\n        SignedInConfig.Home -&gt; HomeNavHostComponent(\n            componentContext = ctx.toAppContext(),\n            navigation = object : HomeNavHostNavigation {\n                override fun toProfile() = stackNavigation.push(SignedInConfig.Profile)\n            },\n        )\n        SignedInConfig.Profile -&gt; ProfileComponent(\n            componentContext = ctx.toAppContext(),\n            navigation = object : ProfileNavigation {\n                override fun back() = stackNavigation.pop()\n            },\n        )\n    }\n}\n</code></pre> <p>Navigation configs must be serializable</p> <p>All navigation configurations must be <code>@Serializable</code> data classes or objects. Decompose uses serialization to preserve the full navigation stack across process death and configuration changes.</p>"},{"location":"architecture/kmp/state/","title":"State Management","text":""},{"location":"architecture/kmp/state/#state-management-kmp","title":"State management (KMP)","text":""},{"location":"architecture/kmp/state/#state-as-a-data-class","title":"State as a data class","text":"<p>In the KMP / Decompose path, state is a plain data class. No marker interface is required.</p> <pre><code>data class HomeState(\n    val title: String = \"\",\n    val isLoading: Boolean = false,\n)\n</code></pre>"},{"location":"architecture/kmp/state/#holding-and-updating-state","title":"Holding and updating state","text":"<p><code>BaseComponent</code> provides <code>componentState: MutableStateFlow&lt;VS&gt;</code> to hold the component's state.</p> <p>Update state using the built-in <code>MutableStateFlow.update</code> extension:</p> <pre><code>componentState.update { copy(title = \"new\") }\n</code></pre> <p>Or use the equivalent Arkitekt helper from <code>app.futured.arkitekt.decompose.ext</code>:</p> <pre><code>update(componentState) { copy(title = \"new\") }\n</code></pre>"},{"location":"architecture/kmp/state/#exposing-state","title":"Exposing state","text":"<p>Define an interface that exposes only what the UI needs, then implement it in your component:</p> <pre><code>interface HomeScreen {\n    val state: StateFlow&lt;HomeState&gt;\n}\n\nclass HomeComponent(\n    componentContext: AppComponentContext,\n) : HomeScreen, BaseComponent&lt;HomeState, HomeUiEvent&gt;(componentContext, HomeState()) {\n\n    override val state: StateFlow&lt;HomeState&gt; = componentState\n}\n</code></pre> <p>This keeps the UI decoupled from the concrete component class, which makes Compose previews and tests straightforward: just provide a fake <code>HomeScreen</code> implementation.</p>"},{"location":"architecture/kmp/state/#observing-state-in-compose","title":"Observing state in Compose","text":"<p>Pass the interface type to your composable, not the concrete component:</p> <pre><code>@Composable\nfun HomeScreen(component: HomeScreen) {\n    val state by component.state.collectAsState()\n\n    if (state.isLoading) {\n        CircularProgressIndicator()\n    } else {\n        Text(state.title)\n    }\n}\n</code></pre>"},{"location":"architecture/kmp/state/#value-flow-conversions","title":"Value / Flow conversions","text":"<p>Arkitekt provides utility extensions for bridging Decompose <code>Value</code> and Kotlin Coroutines <code>StateFlow</code> / <code>Flow</code> APIs.</p>"},{"location":"architecture/kmp/state/#valuetasstateflow","title":"<code>Value&lt;T&gt;.asStateFlow()</code>","text":"<p>Converts a Decompose <code>Value</code> to a Kotlin <code>StateFlow</code>:</p> <pre><code>val state: StateFlow&lt;HomeState&gt; = decomposeValue.asStateFlow()\n</code></pre>"},{"location":"architecture/kmp/state/#flowtcollectasvalueinitial-coroutinescope","title":"<code>Flow&lt;T&gt;.collectAsValue(initial, coroutineScope)</code>","text":"<p>Converts a Kotlin <code>Flow</code> to a Decompose <code>Value</code>:</p> <pre><code>val decomposeValue: Value&lt;HomeState&gt; = stateFlow.collectAsValue(\n    initial = HomeState(),\n    coroutineScope = lifecycleScope,\n)\n</code></pre>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#installation","title":"Installation","text":"<p>All Arkitekt artifacts are published to Maven Central under the group ID <code>app.futured.arkitekt</code>.</p>"},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"Tool Minimum version Android Gradle Plugin 9.1 Gradle 9.4 Kotlin 2.4 JDK 17 <code>minSdk</code> 23 <p>Arkitekt requires AndroidX. Jetifier is not supported.</p>"},{"location":"getting-started/installation/#dependencies","title":"Dependencies","text":"<p>Add the modules you need to your <code>build.gradle.kts</code>:</p> <pre><code>dependencies {\n    // Core ViewModel and ViewState classes (Android)\n    implementation(\"app.futured.arkitekt:core:6.1.0\")\n\n    // Jetpack Compose integration (Android)\n    implementation(\"app.futured.arkitekt:compose:6.1.0\")\n\n    // Coroutine use cases (KMP)\n    implementation(\"app.futured.arkitekt:cr-usecases:6.1.0\")\n\n    // Decompose integration (KMP)\n    implementation(\"app.futured.arkitekt:decompose:6.1.0\")\n\n    // KSP annotation for Decompose factory generation (KMP)\n    implementation(\"app.futured.arkitekt:decompose-annotation:6.1.0\")\n\n    // KSP processor for Decompose factory generation (KMP)\n    ksp(\"app.futured.arkitekt:decompose-processor:6.1.0\")\n\n    // ViewModel testing utilities\n    testImplementation(\"app.futured.arkitekt:core-test:6.1.0\")\n\n    // UseCase mocking utilities\n    testImplementation(\"app.futured.arkitekt:cr-usecases-test:6.1.0\")\n\n    // Decompose component testing utilities (KMP)\n    testImplementation(\"app.futured.arkitekt:decompose-test:6.1.0\")\n}\n</code></pre>"},{"location":"getting-started/installation/#module-selection","title":"Module selection","text":"<p>KMP modules support Android and iOS targets.</p> Module Android iOS <code>core</code> \u2713 \u2717 <code>compose</code> \u2713 \u2717 <code>cr-usecases</code> \u2713 \u2713 <code>decompose</code> \u2713 \u2713 <code>decompose-annotation</code> \u2713 \u2713 <code>decompose-processor</code> \u2713 \u2713 <code>core-test</code> \u2713 \u2717 <code>cr-usecases-test</code> \u2713 \u2713 <code>decompose-test</code> \u2713 \u2713 <p>For Android-only projects, use <code>core</code>, <code>compose</code>, and <code>cr-usecases</code>.</p> <p>For KMP projects, use <code>cr-usecases</code>, <code>decompose</code>, <code>decompose-annotation</code>, and <code>decompose-processor</code>.</p>"},{"location":"getting-started/installation/#snapshot-versions","title":"Snapshot versions","text":"<p>Snapshot builds are published to the Sonatype snapshots repository. To use them, add the snapshot repository to your <code>settings.gradle.kts</code> or <code>build.gradle.kts</code>:</p> <pre><code>repositories {\n    maven(url = \"https://oss.sonatype.org/content/repositories/snapshots\")\n}\n</code></pre> <p>Snapshots are grouped by major version. For example, development builds for the 6.x release line are published as:</p> <pre><code>implementation(\"app.futured.arkitekt:core:6.X.X-SNAPSHOT\")\n</code></pre> <p>Replace <code>6.X.X-SNAPSHOT</code> with the actual snapshot version available in the repository.</p>"},{"location":"getting-started/project-setup-android/","title":"Android","text":""},{"location":"getting-started/project-setup-android/#project-setup-android","title":"Project setup (Android)","text":"<p>This guide walks through the minimal project structure for an Android app using Arkitekt with Jetpack Compose and Hilt.</p>"},{"location":"getting-started/project-setup-android/#project-structure","title":"Project structure","text":"<pre><code>app/\n\u2514\u2500\u2500 src/main\n    \u251c\u2500\u2500 java/com/example/myapp\n    \u2502   \u251c\u2500\u2500 ui\n    \u2502   \u2502   \u251c\u2500\u2500 main\n    \u2502   \u2502   \u2502   \u2514\u2500\u2500 MainActivity.kt\n    \u2502   \u2502   \u2514\u2500\u2500 home\n    \u2502   \u2502       \u251c\u2500\u2500 HomeScreen.kt\n    \u2502   \u2502       \u251c\u2500\u2500 HomeViewModel.kt\n    \u2502   \u2502       \u2514\u2500\u2500 HomeViewState.kt\n    \u2502   \u2514\u2500\u2500 App.kt\n    \u2514\u2500\u2500 res/values/strings.xml\n</code></pre>"},{"location":"getting-started/project-setup-android/#build-configuration","title":"Build configuration","text":"<p>Hilt + Kotlin 2.4</p> <p>Hilt currently bundles an older <code>kotlin-metadata-jvm</code> that may fail to parse Kotlin 2.4 metadata (google/dagger#5001). If you hit a metadata parsing error during the KSP/Hilt build, add the matching version as a KSP dependency so the highest version on the processor classpath wins:</p> <pre><code>dependencies {\n    ksp(\"org.jetbrains.kotlin:kotlin-metadata-jvm:2.4.0\")\n}\n</code></pre> <p>This is a temporary workaround until Hilt updates its bundled version.</p>"},{"location":"getting-started/project-setup-android/#application-class","title":"Application class","text":"<p>Create an <code>Application</code> subclass annotated with <code>@HiltAndroidApp</code>. You can optionally configure global error logging for use cases here.</p> <pre><code>@HiltAndroidApp\nclass App : Application() {\n    override fun onCreate() {\n        super.onCreate()\n        UseCaseErrorHandler.globalOnErrorLogger = { error -&gt;\n            Log.d(\"UseCase\", \"$error\")\n        }\n    }\n}\n</code></pre>"},{"location":"getting-started/project-setup-android/#activity","title":"Activity","text":"<p>The main activity is the Compose entry point. Annotate it with <code>@AndroidEntryPoint</code> for Hilt injection.</p> <pre><code>@AndroidEntryPoint\nclass MainActivity : ComponentActivity() {\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContent {\n            MyAppTheme {\n                HomeScreen()\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"getting-started/project-setup-android/#viewstate","title":"ViewState","text":"<p>ViewState holds the UI state as observable Compose <code>mutableStateOf</code> fields. It implements the <code>ViewState</code> interface from the <code>core</code> module.</p> <pre><code>class HomeViewState @Inject constructor() : ViewState {\n    val title = mutableStateOf(\"\")\n}\n</code></pre>"},{"location":"getting-started/project-setup-android/#viewmodel","title":"ViewModel","text":"<p>The ViewModel extends <code>BaseCoreViewModel</code> and exposes the view state. Annotate with <code>@HiltViewModel</code> for Hilt injection.</p> <pre><code>@HiltViewModel\nclass HomeViewModel @Inject constructor(\n    override val viewState: HomeViewState,\n) : BaseCoreViewModel&lt;HomeViewState&gt;()\n</code></pre>"},{"location":"getting-started/project-setup-android/#screen-composable","title":"Screen composable","text":"<p>The screen composable obtains the ViewModel via <code>hiltViewModel()</code> and reads state directly from the view state fields.</p> <pre><code>@Composable\nfun HomeScreen(\n    viewModel: HomeViewModel = hiltViewModel(),\n) {\n    val title by viewModel.viewState.title\n    Text(text = title)\n}\n</code></pre>"},{"location":"getting-started/project-setup-kmp/","title":"KMP","text":""},{"location":"getting-started/project-setup-kmp/#project-setup-kmp","title":"Project setup (KMP)","text":"<p>This guide walks through the minimal project structure for a Kotlin Multiplatform project using Arkitekt with Decompose for component-based architecture and Koin for dependency injection.</p>"},{"location":"getting-started/project-setup-kmp/#project-structure","title":"Project structure","text":"<pre><code>shared/\n\u2514\u2500\u2500 src/commonMain/kotlin/com/example/myapp\n    \u251c\u2500\u2500 navigation\n    \u2502   \u2514\u2500\u2500 HomeNavigation.kt\n    \u2514\u2500\u2500 ui/home\n        \u251c\u2500\u2500 HomeComponent.kt\n        \u2514\u2500\u2500 HomeState.kt\n\nandroidApp/\n\u2514\u2500\u2500 src/main/kotlin/com/example/myapp\n    \u2514\u2500\u2500 ui/home\n        \u2514\u2500\u2500 HomeScreen.kt\n</code></pre>"},{"location":"getting-started/project-setup-kmp/#state","title":"State","text":"<p>State is a simple data class defined in common code. It represents the UI state for a given component.</p> <pre><code>data class HomeState(val title: String = \"\")\n</code></pre>"},{"location":"getting-started/project-setup-kmp/#navigation","title":"Navigation","text":"<p>Navigation actions are defined as an interface extending <code>NavigationActions</code>. The parent component provides the implementation.</p> <pre><code>interface HomeNavigation : NavigationActions {\n    fun toDetail()\n}\n</code></pre>"},{"location":"getting-started/project-setup-kmp/#component","title":"Component","text":"<p>The component extends <code>BaseComponent</code> and is annotated with <code>@GenerateFactory</code> to enable automatic factory generation via KSP.</p> <pre><code>@GenerateFactory\nclass HomeComponent(\n    @InjectedParam componentContext: AppComponentContext,\n    @InjectedParam navigation: HomeNavigation,\n    private val someUseCase: SomeUseCase,\n) : BaseComponent&lt;HomeState, HomeUiEvent&gt;(componentContext, HomeState()) {\n\n    val state: StateFlow&lt;HomeState&gt; = componentState\n\n    fun onDetailClicked() {\n        navigation.toDetail()\n    }\n}\n</code></pre> <p>The <code>@GenerateFactory</code> annotation triggers KSP to generate a <code>HomeComponentFactory</code> class. This factory resolves Koin dependencies (such as <code>someUseCase</code>) automatically, so you only need to supply <code>@InjectedParam</code> arguments manually when creating the component.</p> <p>For full KSP setup details and advanced configuration, see the Factory Generator page.</p>"},{"location":"getting-started/project-setup-kmp/#screen-composable-android","title":"Screen composable (Android)","text":"<p>On the Android target, the screen composable receives the component directly and collects its state as a Compose state.</p> <pre><code>@Composable\nfun HomeScreen(component: HomeComponent) {\n    val state by component.state.collectAsState()\n    Text(text = state.title)\n\n    EventsEffect(component.events) {\n        onEvent&lt;HomeUiEvent.ShowToast&gt; { /* handle */ }\n    }\n}\n</code></pre>"},{"location":"getting-started/quick-start-android/","title":"Android","text":""},{"location":"getting-started/quick-start-android/#quick-start-android","title":"Quick start (Android)","text":"<p>This walkthrough builds a simple login screen end to end using Arkitekt with Jetpack Compose and Hilt.</p>"},{"location":"getting-started/quick-start-android/#define-the-viewstate","title":"Define the ViewState","text":"<p>The view state holds all observable UI fields using Compose <code>mutableStateOf</code>.</p> <pre><code>class LoginViewState @Inject constructor() : ViewState {\n    var name by mutableStateOf(\"\")\n    var surname by mutableStateOf(\"\")\n    var fullName by mutableStateOf(\"\")\n    var isLoading by mutableStateOf(false)\n}\n</code></pre>"},{"location":"getting-started/quick-start-android/#create-a-use-case","title":"Create a use case","text":"<p>Use cases extend <code>UseCase&lt;Args, Result&gt;</code> from the <code>cr-usecases</code> module. They encapsulate a single unit of business logic.</p> <pre><code>class LoginUseCase @Inject constructor(\n    private val loginStore: LoginStore,\n) : UseCase&lt;LoginData, Unit&gt; {\n\n    override suspend fun build(args: LoginData) {\n        loginStore.login(args.name, args.surname)\n    }\n}\n</code></pre>"},{"location":"getting-started/quick-start-android/#build-the-viewmodel","title":"Build the ViewModel","text":"<p>The ViewModel extends <code>BaseViewModel&lt;LoginViewState&gt;</code> from the <code>compose</code> module. <code>BaseViewModel</code> extends <code>BaseCoreViewModel</code> and adds <code>CoroutineScopeOwner</code>, which is needed to execute use cases with the <code>execute()</code> extension.</p> <pre><code>@HiltViewModel\nclass LoginViewModel @Inject constructor(\n    override val viewState: LoginViewState,\n    private val loginUseCase: LoginUseCase,\n) : BaseViewModel&lt;LoginViewState&gt;() {\n\n    fun logIn() {\n        val args = LoginData(\n            name = viewState.name.value,\n            surname = viewState.surname.value,\n        )\n        loginUseCase.execute(args) {\n            onStart {\n                viewState.isLoading = true\n            }\n            onSuccess {\n                viewState.isLoading = false\n                viewState.fullName = \"${args.name} ${args.surname}\"\n            }\n            onError { error -&gt;\n                viewState.isLoading = false\n                sendEvent(LoginEvent.ShowError(error.message.orEmpty()))\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"getting-started/quick-start-android/#compose-the-screen","title":"Compose the screen","text":"<p>The screen composable obtains the ViewModel via <code>hiltViewModel()</code>, observes state, and calls ViewModel functions on user interaction.</p> <pre><code>@Composable\nfun LoginScreen(\n    viewModel: LoginViewModel = hiltViewModel(),\n) {\n    val name = viewModel.viewState.name\n    val surname = viewModel.viewState.surname\n    val fullName = viewModel.viewState.fullName\n    val isLoading = viewModel.viewState.isLoading\n\n    Column {\n        TextField(\n            value = name,\n            onValueChange = { viewModel.viewState.name = it },\n            label = { Text(\"Name\") },\n        )\n        TextField(\n            value = surname,\n            onValueChange = { viewModel.viewState.surname = it },\n            label = { Text(\"Surname\") },\n        )\n\n        Button(\n            onClick = { viewModel.logIn() },\n            enabled = !isLoading,\n        ) {\n            Text(\"Log In\")\n        }\n\n        if (fullName.isNotEmpty()) {\n            Text(text = \"Welcome, $fullName!\")\n        }\n    }\n\n    viewModel.EventsEffect {\n        onEvent&lt;LoginEvent.ShowError&gt; { event -&gt;\n            // Show error snackbar or toast\n        }\n    }\n}\n</code></pre> <p>Use <code>EventsEffect</code> to handle one-shot events dispatched by the ViewModel via <code>sendEvent()</code>. Events are collected only while the composable is in the composition.</p> <p>See it in a full project</p> <p>This repo's <code>example</code> module shows these patterns in a minimal Android app. For a complete project setup, see the Android Project Template.</p>"},{"location":"getting-started/quick-start-kmp/","title":"KMP","text":""},{"location":"getting-started/quick-start-kmp/#quick-start-kmp","title":"Quick start (KMP)","text":"<p>This walkthrough builds a simple login feature end to end using Arkitekt with Decompose and Koin in a Kotlin Multiplatform project.</p>"},{"location":"getting-started/quick-start-kmp/#define-the-state","title":"Define the state","text":"<p>State is a plain data class in common code.</p> <pre><code>data class LoginState(\n    val name: String = \"\",\n    val surname: String = \"\",\n    val fullName: String = \"\",\n    val isLoading: Boolean = false,\n)\n</code></pre>"},{"location":"getting-started/quick-start-kmp/#define-navigation-actions","title":"Define navigation actions","text":"<p>Navigation actions are declared as an interface extending <code>NavigationActions</code>. The parent component provides the implementation.</p> <pre><code>interface LoginNavigation : NavigationActions {\n    fun toHome()\n}\n</code></pre>"},{"location":"getting-started/quick-start-kmp/#define-ui-events","title":"Define UI events","text":"<p>One-shot events are modeled as a sealed interface extending <code>UiEvent</code>.</p> <pre><code>sealed interface LoginUiEvent : UiEvent {\n    data class ShowError(val message: String) : LoginUiEvent\n}\n</code></pre>"},{"location":"getting-started/quick-start-kmp/#build-the-component","title":"Build the component","text":"<p>The component extends <code>BaseComponent&lt;LoginState, LoginUiEvent&gt;</code> and is annotated with <code>@GenerateFactory</code> for automatic factory generation.</p> <p>To execute use cases, the component implements <code>CoroutineScopeOwner</code> and provides <code>lifecycleScope</code> as <code>useCaseScope</code>.</p> <pre><code>@GenerateFactory\nclass LoginComponent(\n    @InjectedParam componentContext: AppComponentContext,\n    @InjectedParam navigation: LoginNavigation,\n    private val loginUseCase: LoginUseCase,\n) : BaseComponent&lt;LoginState, LoginUiEvent&gt;(componentContext, LoginState()),\n    CoroutineScopeOwner {\n\n    override val useCaseScope = lifecycleScope\n    override val useCaseJobPool = mutableMapOf&lt;Any, Job&gt;()\n\n    val state: StateFlow&lt;LoginState&gt; = componentState\n\n    fun onNameChanged(name: String) {\n        update(componentState) { copy(name = name) }\n    }\n\n    fun onSurnameChanged(surname: String) {\n        update(componentState) { copy(surname = surname) }\n    }\n\n    fun logIn() {\n        val currentState = componentState.value\n        val args = LoginData(\n            name = currentState.name,\n            surname = currentState.surname,\n        )\n        loginUseCase.execute(args) {\n            onStart {\n                update(componentState) { copy(isLoading = true) }\n            }\n            onSuccess {\n                update(componentState) {\n                    copy(\n                        isLoading = false,\n                        fullName = \"${args.name} ${args.surname}\",\n                    )\n                }\n            }\n            onError { error -&gt;\n                update(componentState) { copy(isLoading = false) }\n                sendUiEvent(LoginUiEvent.ShowError(error.message.orEmpty()))\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"getting-started/quick-start-kmp/#compose-the-screen-android","title":"Compose the screen (Android)","text":"<p>On the Android target, the screen composable receives the component, collects its state, and uses <code>EventsEffect</code> from the <code>decompose</code> module to handle one-shot events.</p> <pre><code>@Composable\nfun LoginScreen(component: LoginComponent) {\n    val state by component.state.collectAsState()\n\n    Column {\n        TextField(\n            value = state.name,\n            onValueChange = { component.onNameChanged(it) },\n            label = { Text(\"Name\") },\n        )\n        TextField(\n            value = state.surname,\n            onValueChange = { component.onSurnameChanged(it) },\n            label = { Text(\"Surname\") },\n        )\n\n        Button(\n            onClick = { component.logIn() },\n            enabled = !state.isLoading,\n        ) {\n            Text(\"Log In\")\n        }\n\n        if (state.fullName.isNotEmpty()) {\n            Text(text = \"Welcome, ${state.fullName}!\")\n        }\n    }\n\n    EventsEffect(component.events) {\n        onEvent&lt;LoginUiEvent.ShowError&gt; { event -&gt;\n            // Show error snackbar or toast\n        }\n    }\n}\n</code></pre>"},{"location":"getting-started/quick-start-kmp/#koin-module-setup","title":"Koin module setup","text":"<p>Register the use case in a Koin module so the generated <code>LoginComponentFactory</code> can resolve it automatically.</p> <pre><code>val loginModule = module {\n    factory { LoginUseCase(get()) }\n}\n</code></pre> <p>The <code>@GenerateFactory</code> annotation generates a <code>LoginComponentFactory</code> that resolves all non-<code>@InjectedParam</code> constructor parameters from Koin. You only supply <code>componentContext</code> and <code>navigation</code> manually when creating the component. See Factory Generator for details.</p> <p>See it in a full project</p> <p>The KMP Futured Template is a complete working app (Android + iOS) built with these same patterns: BaseComponent, Koin, Decompose navigation, and KSP factory generation.</p>"},{"location":"migration/android/","title":"Migration","text":""},{"location":"migration/android/#android-migration-guide-5x-to-6x","title":"Android migration guide (5.x to 6.x)","text":"<p>Version 6.x is a major release focused on modern Android development with Jetpack Compose and Kotlin Coroutines.</p>"},{"location":"migration/android/#toolchain-requirements","title":"Toolchain requirements","text":"<p>Upgrade your project to meet the minimum requirements:</p> Tool Required version Android Gradle Plugin 9.1 Gradle 9.4 Kotlin 2.4 JDK 17 <code>compileSdk</code> / <code>targetSdk</code> 36 <code>minSdk</code> 23 <p>Remove multidex (not needed with <code>minSdk</code> \u2265 21) and remove Jetifier (<code>android.enableJetifier</code>) from <code>gradle.properties</code>. Arkitekt is AndroidX only.</p> <p>Hilt + Kotlin 2.4</p> <p>Hilt currently bundles an older <code>kotlin-metadata-jvm</code> that may fail to parse Kotlin 2.4 metadata (google/dagger#5001). If you hit a metadata parsing error during the KSP/Hilt build, add the matching version as a KSP dependency so the highest version on the processor classpath wins:</p> <pre><code>dependencies {\n    ksp(\"org.jetbrains.kotlin:kotlin-metadata-jvm:2.4.0\")\n}\n</code></pre> <p>This is a temporary workaround until Hilt updates its bundled version.</p>"},{"location":"migration/android/#removed-modules","title":"Removed modules","text":"Module Replacement <code>rx-usecases</code>, <code>rx-usecases-test</code> <code>cr-usecases</code> (Coroutines) <code>dagger</code> Dagger-Hilt (<code>@HiltViewModel</code>, <code>@AndroidEntryPoint</code>) <code>bindingadapters</code> Jetpack Compose <code>example-minimal</code>, <code>example-hilt</code> Consolidated <code>example</code> module"},{"location":"migration/android/#removed-classes","title":"Removed classes","text":""},{"location":"migration/android/#viewmodel-base-classes","title":"ViewModel base classes","text":"Removed Replacement <code>BaseViewModel</code> (from core) <code>BaseCoreViewModel</code> (core) or <code>BaseViewModel</code> (compose) <code>BaseCrViewModel</code> (from cr-usecases) <code>BaseViewModel</code> (compose)"},{"location":"migration/android/#fragment-activity-base-classes","title":"Fragment / Activity base classes","text":"Removed Replacement <code>ViewModelActivity</code>, <code>BindingViewModelActivity</code> <code>ComponentActivity</code> with <code>@AndroidEntryPoint</code> <code>ViewModelFragment</code>, <code>BindingViewModelFragment</code> Compose navigation <code>ViewModelBottomSheetDialogFragment</code>, <code>BindingViewModelBottomSheetDialogFragment</code> Compose bottom sheets <code>ViewModelDialogFragment</code>, <code>BindingViewModelDialogFragment</code> Compose dialogs"},{"location":"migration/android/#dagger-classes","title":"Dagger classes","text":"Removed Replacement <code>BaseViewModelFactory</code>, <code>BaseSavedStateViewModelFactory</code> <code>@HiltViewModel</code> with <code>hiltViewModel()</code> <code>BaseDaggerActivity</code>, <code>BaseDaggerFragment</code> and Binding variants <code>@AndroidEntryPoint</code> <code>ViewModelCreator</code>, <code>ViewModelFactory</code> No longer needed with Hilt"},{"location":"migration/android/#livedata-components","title":"LiveData components","text":"Removed Replacement <code>LiveEvent</code>, <code>LiveEventBus</code> <code>Event</code> with Channel-based events <code>DefaultValueLiveData</code>, <code>DefaultValueMediatorLiveData</code> <code>StateFlow</code> or Compose <code>State</code> <code>NonNullLiveData</code> <code>StateFlow</code> or Compose <code>State</code> <code>UiData</code>, <code>UiDataExtensions</code>, <code>UiDataMediator</code> <code>StateFlow</code> or Compose <code>State</code> <code>LiveDataExtensions</code>, <code>LiveDataUtils</code> Kotlin Flow operators"},{"location":"migration/android/#databinding","title":"DataBinding","text":"<p>All DataBinding support has been removed. Use Jetpack Compose instead.</p>"},{"location":"migration/android/#usecase-api-changes","title":"UseCase API changes","text":""},{"location":"migration/android/#usecase-is-now-an-interface","title":"UseCase is now an interface","text":"<p><code>UseCase&lt;ARGS, T&gt;</code> changed from an abstract class to an interface. Update all definitions:</p> <pre><code>// Before\nclass LoginUseCase : UseCase&lt;LoginData, User&gt;() { ... }\n\n// After\nclass LoginUseCase : UseCase&lt;LoginData, User&gt; { ... }\n</code></pre> <p>The <code>deferred</code> property that tracked cancellation has been removed. Cancellation is now managed internally via <code>CoroutineScopeOwner.useCaseJobPool</code>.</p>"},{"location":"migration/android/#flowusecase-is-now-an-interface","title":"FlowUseCase is now an interface","text":"<p>Same change applies to <code>FlowUseCase</code>:</p> <pre><code>// Before\nclass ObserveUserUseCase : FlowUseCase&lt;Unit, User&gt;() { ... }\n\n// After\nclass ObserveUserUseCase : FlowUseCase&lt;Unit, User&gt; { ... }\n</code></pre> <p>The <code>job</code> property has been removed. The running job is stored in <code>useCaseJobPool</code>.</p>"},{"location":"migration/android/#executemapped-removed","title":"executeMapped removed","text":"<p><code>executeMapped</code> has been removed from <code>FlowUseCase</code>. Apply transformations inside <code>build()</code> instead:</p> <pre><code>// Before\nflowUseCase.executeMapped(Unit, transform = { it.name }) {\n    onNext { name -&gt; /* ... */ }\n}\n\n// After\nclass ObserveUserNamesUseCase : FlowUseCase&lt;Unit, String&gt; {\n    override fun build(args: Unit): Flow&lt;String&gt; =\n        userStore.observeUsers().map { it.name }\n}\n</code></pre>"},{"location":"migration/android/#execute-uses-context-parameters","title":"execute uses context parameters","text":"<p>The <code>execute</code> extensions are now declared with Kotlin context parameters. Context parameters are stable as of Kotlin 2.4, so no compiler flag is needed. Call <code>execute(...)</code> directly from inside any <code>CoroutineScopeOwner</code> (e.g. a ViewModel or Component) and the receiver is resolved automatically. The call sites remain syntactically the same as before:</p> <pre><code>loginUseCase.execute(args) { onSuccess { /* ... */ } }\n</code></pre>"},{"location":"migration/android/#coroutinescopeowner-changes","title":"CoroutineScopeOwner changes","text":""},{"location":"migration/android/#coroutinescope-renamed-to-usecasescope","title":"coroutineScope renamed to useCaseScope","text":"<p>The <code>coroutineScope</code> property on <code>CoroutineScopeOwner</code> has been renamed to <code>useCaseScope</code>. Update any custom <code>CoroutineScopeOwner</code> implementations:</p> <pre><code>// Before\noverride val coroutineScope: CoroutineScope = viewModelScope\n\n// After\noverride val useCaseScope: CoroutineScope = viewModelScope\n</code></pre>"},{"location":"migration/android/#usecasejobpool-is-now-required","title":"useCaseJobPool is now required","text":"<p>Custom <code>CoroutineScopeOwner</code> implementations must also provide <code>useCaseJobPool</code>:</p> <pre><code>override val useCaseJobPool: MutableMap&lt;Any, Job&gt; = mutableMapOf()\n</code></pre> <p><code>BaseViewModel</code> and <code>BaseCoreViewModel</code> from Arkitekt already provide both, so only custom implementations need to be updated.</p>"},{"location":"migration/android/#result-type-migration","title":"Result type migration","text":"<p>Arkitekt's custom <code>Result&lt;VALUE&gt;</code> sealed class has been removed. The sync-execute variant now returns <code>kotlin.Result&lt;T&gt;</code>.</p>"},{"location":"migration/android/#replacing-extension-functions","title":"Replacing extension functions","text":"Old (Arkitekt Result) New (kotlin.Result) <code>.getOrNull()</code> <code>.getOrNull()</code> \u2713 built-in <code>.getOrDefault(v)</code> <code>.getOrDefault(v)</code> \u2713 built-in <code>.getOrElse { }</code> <code>.getOrElse { }</code> \u2713 built-in <code>.getOrThrow()</code> <code>.getOrThrow()</code> \u2713 built-in <code>.getOrCancel { }</code> <code>.getOrCancel { }</code> from <code>cr-usecases</code> <code>.map { }</code> <code>.map { }</code> \u2713 built-in <code>.fold(onSuccess, onError)</code> <code>.fold(onSuccess, onFailure)</code> \u2713 built-in <code>tryCatch { }</code> <code>runCatching { }</code> \u2713 built-in"},{"location":"migration/android/#destructuring","title":"Destructuring","text":"<p><code>kotlin.Result</code> does not support destructuring declarations. Replace any <code>val (value, error) = result</code> calls with explicit <code>getOrNull()</code> / <code>exceptionOrNull()</code> calls.</p>"},{"location":"migration/android/#migration-path","title":"Migration path","text":"<ol> <li>Upgrade toolchain to AGP 9.1, Gradle 9.4, Kotlin 2.4, JDK 17</li> <li>Remove multidex and Jetifier from <code>gradle.properties</code></li> <li>Add <code>ksp(\"org.jetbrains.kotlin:kotlin-metadata-jvm:2.4.0\")</code> if you hit Hilt metadata parsing errors (google/dagger#5001)</li> <li>Replace Fragment/Activity base classes with <code>ComponentActivity</code> + <code>@AndroidEntryPoint</code></li> <li>Replace LiveData with <code>StateFlow</code> or Compose <code>State</code></li> <li>Replace <code>LiveEvent</code> with Channel-based <code>Event</code> system</li> <li>Migrate UI to Jetpack Compose</li> <li>Replace old <code>BaseViewModel</code> with <code>BaseCoreViewModel</code> or <code>BaseViewModel</code></li> <li>Replace Dagger 2 with Dagger-Hilt</li> <li>Update UseCase/FlowUseCase from abstract class to interface (remove <code>()</code> from supertype)</li> <li>Remove <code>executeMapped</code> calls and apply mapping in <code>build()</code> instead</li> <li>Rename <code>coroutineScope</code> \u2192 <code>useCaseScope</code> in custom <code>CoroutineScopeOwner</code> implementations; add <code>useCaseJobPool</code></li> <li>Replace custom <code>Result</code> usage with <code>kotlin.Result</code> extensions</li> </ol>"},{"location":"testing/component-testing/","title":"Component Testing","text":""},{"location":"testing/component-testing/#component-testing","title":"Component testing","text":"<p>The <code>decompose-test</code> module provides infrastructure for unit-testing Decompose components in Kotlin Multiplatform projects.</p>"},{"location":"testing/component-testing/#dependency","title":"Dependency","text":"<pre><code>// commonMain test source set (or platform-specific test source set)\ntestImplementation(\"app.futured.arkitekt:decompose-test:6.1.0\")\n</code></pre> <p>For KMP modules, add it to the test source set in your <code>build.gradle.kts</code>:</p> <pre><code>kotlin {\n    sourceSets {\n        commonTest {\n            dependencies {\n                implementation(\"app.futured.arkitekt:decompose-test:6.1.0\")\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"testing/component-testing/#componenttest","title":"ComponentTest","text":"<p><code>ComponentTest</code> is an interface that wires up the testing environment for a <code>BaseComponent</code>:</p> Member Type Description <code>testScope</code> <code>TestScope</code> Shared coroutine scope for the test body and <code>runComponentTest</code> <code>testDispatcher</code> <code>TestDispatcher</code> Installed as <code>Dispatchers.Main</code> during each test <code>lifecycleRegistry</code> <code>LifecycleRegistry</code> Drives the component lifecycle; call <code>lifecycleRegistry.create()</code> to start it <code>componentContext</code> <code>DefaultComponentContext</code> Pass this to the component under test <code>setup()</code> <code>@BeforeTest</code> Installs <code>testDispatcher</code> as <code>Dispatchers.Main</code> <code>cleanup()</code> <code>@AfterTest</code> Destroys the lifecycle (if alive) and resets <code>Dispatchers.Main</code>"},{"location":"testing/component-testing/#componenttestpreparation","title":"ComponentTestPreparation","text":"<p><code>ComponentTestPreparation</code> is the default implementation. Use Kotlin class delegation to wire up your test class:</p> <pre><code>class LoginComponentTest : ComponentTest by ComponentTestPreparation() {\n\n    @Test\n    fun `state updates after login`() = runComponentTest {\n        lifecycleRegistry.create()\n        val component = LoginComponent(\n            componentContext = componentContext,\n            navigation = FakeLoginNavigation(),\n            loginUseCase = FakeLoginUseCase(),\n        )\n        component.logIn()\n        runCurrent()\n\n        assertEquals(\"John Doe\", component.state.value.fullName)\n    }\n}\n</code></pre> <p><code>ComponentTestPreparation</code> is <code>open</code>, so you can subclass it to add project-specific fixtures or helpers shared across multiple test classes.</p>"},{"location":"testing/component-testing/#runcomponenttest","title":"runComponentTest","text":"<p><code>runComponentTest</code> is a helper that runs your test body inside <code>testScope</code>:</p> <pre><code>fun runComponentTest(\n    timeout: Duration = 60.seconds,\n    testBody: suspend TestScope.() -&gt; Unit,\n)\n</code></pre> <p>It delegates to <code>testScope.runTest(timeout, testBody)</code>. Use it as the test body launcher instead of <code>runTest</code> directly so the scope and dispatcher match the component under test.</p>"},{"location":"testing/component-testing/#driving-the-lifecycle","title":"Driving the lifecycle","text":"<p>The component lifecycle is advanced by calling Essenty lifecycle extensions on <code>lifecycleRegistry</code>:</p> <pre><code>lifecycleRegistry.create()   // INITIALIZED \u2192 CREATED\nlifecycleRegistry.start()    // CREATED \u2192 STARTED\nlifecycleRegistry.resume()   // STARTED \u2192 RESUMED\nlifecycleRegistry.stop()     // RESUMED \u2192 STARTED\nlifecycleRegistry.destroy()  // \u2192 DESTROYED (cancels lifecycleScope, closes events channel)\n</code></pre> <p>Note</p> <p><code>cleanup()</code> automatically destroys the lifecycle after each test if it hasn't been destroyed yet.</p>"},{"location":"testing/component-testing/#testing-state","title":"Testing state","text":"<p>Assert on <code>componentState</code> (exposed as a <code>StateFlow</code>) to verify state changes:</p> <pre><code>@Test\nfun `initial state`() = runComponentTest {\n    lifecycleRegistry.create()\n    val component = createComponent()\n\n    assertEquals(LoginState(), component.state.value)\n}\n</code></pre> <p>Tip</p> <p>Turbine provides a nicer API for testing <code>Flow</code> and <code>StateFlow</code> emissions, including <code>componentState</code> updates.</p>"},{"location":"testing/component-testing/#testing-events","title":"Testing events","text":"<p>Collect events inside <code>backgroundScope</code> to capture one-shot events:</p> <pre><code>@Test\nfun `error event is emitted on failure`() = runComponentTest {\n    lifecycleRegistry.create()\n    val component = createComponent()\n\n    val received = mutableListOf&lt;LoginUiEvent&gt;()\n    backgroundScope.launch(UnconfinedTestDispatcher(testScheduler)) {\n        component.events.collect { received.add(it) }\n    }\n\n    component.logIn() // triggers an error\n    runCurrent()\n\n    assertEquals(listOf(LoginUiEvent.ShowError(\"Auth failed\")), received)\n}\n</code></pre>"},{"location":"testing/component-testing/#testing-lifecycle-teardown","title":"Testing lifecycle teardown","text":"<p>Verify that the component cleans up correctly on destroy:</p> <pre><code>@Test\nfun `events flow completes on destroy`() = runComponentTest {\n    val scope = CoroutineScope(testDispatcher + Job())\n    val component = LoginComponent(componentContext, scope, FakeLoginNavigation(), FakeLoginUseCase())\n    lifecycleRegistry.create()\n\n    var completed = false\n    backgroundScope.launch(UnconfinedTestDispatcher(testScheduler)) {\n        component.events.onCompletion { completed = true }.collect { }\n    }\n\n    lifecycleRegistry.destroy()\n\n    assertTrue(completed)\n}\n</code></pre>"},{"location":"testing/usecase-mocking/","title":"UseCase Mocking","text":""},{"location":"testing/usecase-mocking/#usecase-mocking","title":"UseCase mocking","text":"<p>Android only</p> <p>The utilities described on this page are provided by the <code>cr-usecases-test</code> module and are available on Android only. For KMP projects, see KMP alternatives below.</p> <p>The <code>cr-usecases-test</code> module provides mock extensions for use cases. Since <code>execute</code> is a context parameter extension (not a regular method on the use case), standard MockK mocking won't work. The helpers work by mocking <code>build()</code> directly instead, so use the provided utilities.</p>"},{"location":"testing/usecase-mocking/#dependency","title":"Dependency","text":"<pre><code>testImplementation(\"app.futured.arkitekt:cr-usecases-test:6.1.0\")\n</code></pre>"},{"location":"testing/usecase-mocking/#usecase-mocking_1","title":"UseCase mocking","text":"<pre><code>val mockLoginUseCase: LoginUseCase = mockk()\n\n// Mock with specific args\nmockLoginUseCase.mockExecute(args = LoginData(\"John\", \"Doe\")) { user }\n\n// Mock with any args\nmockLoginUseCase.mockExecute { user }\n\n// Mock with nullable args\nmockLoginUseCase.mockExecuteNullable(args = null) { user }\nmockLoginUseCase.mockExecuteNullable { user }\n</code></pre>"},{"location":"testing/usecase-mocking/#flowusecase-mocking","title":"FlowUseCase mocking","text":"<pre><code>val mockObserveUseCase: ObserveUserUseCase = mockk()\n\n// Mock with specific args\nmockObserveUseCase.mockExecute(args = Unit) { flowOf(\"John Doe\") }\n\n// Mock with any args\nmockObserveUseCase.mockExecute { flowOf(\"John Doe\") }\n</code></pre>"},{"location":"testing/usecase-mocking/#full-test-example","title":"Full test example","text":"<pre><code>class FormViewModelTest : ViewModelTest() {\n\n    private val mockSaveUseCase: SaveFormUseCase = mockk()\n    private val mockObserveUseCase: ObserveFormUseCase = mockk()\n    private lateinit var viewState: FormViewState\n    private lateinit var viewModel: FormViewModel\n\n    @Before\n    fun setUp() {\n        viewState = FormViewState()\n        mockObserveUseCase.mockExecute { flowOf(\"Alice\" to \"Smith\") }\n        mockSaveUseCase.mockExecute { \"Alice\" to \"Smith\" }\n        viewModel = spyk(\n            FormViewModel(mockSaveUseCase, mockObserveUseCase, viewState),\n            recordPrivateCalls = true,\n        ).also {\n            every { it.getWorkerDispatcher() } returns Dispatchers.Main\n        }\n    }\n}\n</code></pre> <p>This pattern ensures all use cases execute synchronously on the test dispatcher and return predictable results.</p>"},{"location":"testing/usecase-mocking/#kmp-alternatives","title":"KMP alternatives","text":"<p>On KMP, use cases are plain interfaces, so the preferred approach is writing fakes: simple in-test implementations that return controlled results without any mocking framework.</p> <pre><code>class FakeLoginUseCase : LoginUseCase {\n    var result: User = User(\"John\", \"Doe\")\n\n    override fun build(args: LoginData): User = result\n}\n</code></pre> <p>If you prefer a mocking framework, Mokkery supports Kotlin Multiplatform and can mock interfaces across all targets.</p> <pre><code>val mockLoginUseCase = mock&lt;LoginUseCase&gt;()\n\neverySuspend { mockLoginUseCase.build(any()) } returns user\n</code></pre> <p>Refer to the Mokkery documentation for setup and full API reference.</p>"},{"location":"testing/viewmodel-testing/","title":"ViewModel Testing","text":""},{"location":"testing/viewmodel-testing/#viewmodel-testing","title":"ViewModel testing","text":"<p>The <code>core-test</code> module provides a <code>ViewModelTest</code> base class that sets up the testing environment for Arkitekt ViewModels.</p>"},{"location":"testing/viewmodel-testing/#dependency","title":"Dependency","text":"<pre><code>testImplementation(\"app.futured.arkitekt:core-test:6.1.0\")\n</code></pre>"},{"location":"testing/viewmodel-testing/#setup","title":"Setup","text":"<p><code>ViewModelTest</code> configures:</p> <ul> <li><code>InstantTaskExecutorRule</code>: makes LiveData operations synchronous</li> <li><code>CoroutineScopeRule</code>: sets the <code>Main</code> dispatcher to <code>UnconfinedTestDispatcher</code> for immediate coroutine execution</li> </ul>"},{"location":"testing/viewmodel-testing/#basic-test","title":"Basic test","text":"<pre><code>class DetailViewModelTest : ViewModelTest() {\n\n    private lateinit var viewModel: DetailViewModel\n    private lateinit var viewState: DetailViewState\n\n    @Before\n    fun setUp() {\n        viewState = DetailViewState()\n        viewModel = DetailViewModel(viewState)\n    }\n\n    @Test\n    fun `increment number updates state`() {\n        viewModel.incrementNumber()\n        assertEquals(1, viewState.number.value)\n    }\n}\n</code></pre>"},{"location":"testing/viewmodel-testing/#testing-events","title":"Testing events","text":"<p>Use MockK's <code>spyk</code> to verify that events are sent:</p> <pre><code>viewModel = spyk(HomeViewModel(viewState), recordPrivateCalls = true)\n\nviewModel.onDetail()\nverify { viewModel.sendEvent(ShowDetailEvent) }\n</code></pre>"},{"location":"testing/viewmodel-testing/#overriding-the-worker-dispatcher","title":"Overriding the worker dispatcher","text":"<p>By default, use cases run on <code>Dispatchers.IO</code>. In tests, override <code>getWorkerDispatcher()</code> to run everything on the Main (test) dispatcher:</p> <pre><code>every { viewModel.getWorkerDispatcher() } returns Dispatchers.Main\n</code></pre> <p>This ensures use case execution is synchronous and predictable in tests.</p>"},{"location":"use-cases/error-handling/","title":"Error Handling","text":""},{"location":"use-cases/error-handling/#error-handling","title":"Error handling","text":"<p>Arkitekt provides several layers of error handling for use cases and coroutine execution.</p>"},{"location":"use-cases/error-handling/#global-error-logger","title":"Global error logger","text":"<p><code>UseCaseErrorHandler.globalOnErrorLogger</code> is a global callback for logging errors. Set it in your <code>Application.onCreate</code>:</p> <pre><code>UseCaseErrorHandler.globalOnErrorLogger = { error -&gt;\n    Logger.logError(error)\n}\n</code></pre>"},{"location":"use-cases/error-handling/#when-is-globalonerrorlogger-called","title":"When is <code>globalOnErrorLogger</code> called?","text":"Execution mode Condition Called? Async <code>execute</code> <code>onError</code> defined Yes (error is logged AND passed to <code>onError</code>) Async <code>execute</code> <code>onError</code> not defined No (exception is thrown/unhandled) <code>launchWithHandler</code> Non-<code>CancellationException</code> Yes <code>launchWithHandler</code> <code>CancellationException</code> with non-cancellation root cause (e.g. via <code>getOrCancel</code>) Yes <code>launchWithHandler</code> Pure <code>CancellationException</code> No"},{"location":"use-cases/error-handling/#launchwithhandler","title":"<code>launchWithHandler</code>","text":"<p><code>launchWithHandler</code> launches a coroutine with built-in try-catch error handling:</p> <pre><code>fun onButtonClicked() = launchWithHandler {\n    val data = loadDataUseCase.execute(args).getOrThrow()\n    // process data\n}\n</code></pre> <p>Error handling behavior:</p> <ul> <li>Non-<code>CancellationException</code>: calls <code>globalOnErrorLogger</code>, then calls <code>defaultErrorHandler</code></li> <li><code>CancellationException</code> caused by <code>getOrCancel</code>: calls <code>globalOnErrorLogger</code> with the original exception</li> <li>Pure <code>CancellationException</code>: silently ignored</li> </ul>"},{"location":"use-cases/error-handling/#defaulterrorhandler","title":"<code>defaultErrorHandler</code>","text":"<p><code>defaultErrorHandler</code> is called by <code>launchWithHandler</code> after <code>globalOnErrorLogger</code> for non-cancellation exceptions. By default, it rethrows the exception.</p> <p>Override it in your ViewModel or Component to handle errors without rethrowing:</p> <pre><code>override fun defaultErrorHandler(exception: Throwable) {\n    viewState.error = exception.message\n}\n</code></pre>"},{"location":"use-cases/error-handling/#getorcancel","title":"<code>getOrCancel</code>","text":"<p><code>getOrCancel</code> is an extension on <code>kotlin.Result&lt;VALUE&gt;</code> (from <code>cr-usecases</code>). It is the recommended way to abort a coroutine cleanly when a sequential use case call fails:</p> <pre><code>fun onButtonClicked() = launchWithHandler {\n    val user = loginUseCase\n        .execute(LoginData(name, password))\n        .getOrCancel { error -&gt; showError(error.message) } // side-effect before cancel\n\n    // only reached on success\n    navigateToHome(user)\n}\n</code></pre> <p>On success, <code>getOrCancel</code> returns the value. On failure, it:</p> <ol> <li>Calls the optional <code>doBeforeThrow</code> lambda (skipped if the exception is already a <code>CancellationException</code>)</li> <li>Throws a <code>CancellationException</code> whose <code>cause</code> is the original exception</li> </ol> <p>The thrown <code>CancellationException</code> bubbles up through <code>launchWithHandler</code>, which logs the original cause via <code>globalOnErrorLogger</code> and then lets the coroutine cancel cleanly. <code>defaultErrorHandler</code> is not called.</p>"},{"location":"use-cases/flow-usecase/","title":"FlowUseCase","text":""},{"location":"use-cases/flow-usecase/#flowusecase","title":"FlowUseCase","text":"<p><code>FlowUseCase&lt;ARGS, T&gt;</code> is an interface for streaming operations that emit multiple values over time.</p>"},{"location":"use-cases/flow-usecase/#defining-a-flowusecase","title":"Defining a FlowUseCase","text":"<p>Implement the <code>build</code> method returning a <code>Flow&lt;T&gt;</code>:</p> <pre><code>class ObserveFormUseCase @Inject constructor(\n    private val formStore: FormStore,\n) : FlowUseCase&lt;Unit, Pair&lt;String, String&gt;&gt; {\n    override fun build(args: Unit): Flow&lt;Pair&lt;String, String&gt;&gt; =\n        formStore.getFormFlow()\n}\n</code></pre>"},{"location":"use-cases/flow-usecase/#execution","title":"Execution","text":"<p>Execute with callback handlers to react to each emitted value. Requires a <code>CoroutineScopeOwner</code> in scope:</p> <pre><code>observeUserUseCase.execute(Unit) {\n    onStart { /* called before flow starts */ }\n    onNext { value -&gt; /* called for each emitted value */ }\n    onError { error -&gt; /* called on error */ }\n    onComplete { /* called when flow completes without error */ }\n    disposePrevious(true) // default\n}\n</code></pre> <p>For <code>Unit</code> args, omit the args parameter:</p> <pre><code>observeUserUseCase.execute {\n    onNext { value -&gt; /* ... */ }\n}\n</code></pre>"},{"location":"use-cases/flow-usecase/#callback-options","title":"Callback options","text":"<ul> <li><code>onStart</code>: called before the flow collection begins</li> <li><code>onNext(T)</code>: called for each value emitted by the flow</li> <li><code>onError(Throwable)</code>: called when the flow throws an error; also triggers <code>UseCaseErrorHandler.globalOnErrorLogger</code></li> <li><code>onComplete</code>: called when the flow completes successfully (without error)</li> <li><code>disposePrevious</code>: whether to cancel any previous flow collection before starting a new one (default <code>true</code>)</li> </ul> <p>The running <code>Job</code> is stored in <code>CoroutineScopeOwner.useCaseJobPool</code> keyed by the use case instance. It is cancelled automatically when <code>disposePrevious</code> is <code>true</code> or when <code>useCaseScope</code> is cancelled.</p>"},{"location":"use-cases/flow-usecase/#transforming-emitted-values","title":"Transforming emitted values","text":"<p>To transform values before <code>onNext</code>, apply Flow operators inside <code>build</code>:</p> <pre><code>class ObserveUserNamesUseCase @Inject constructor(\n    private val userStore: UserStore,\n) : FlowUseCase&lt;Unit, String&gt; {\n    override fun build(args: Unit): Flow&lt;String&gt; =\n        userStore.observeUsers().map { it.fullName }\n}\n</code></pre>"},{"location":"use-cases/flow-usecase/#common-patterns","title":"Common patterns","text":"<p><code>FlowUseCase</code> is commonly used for:</p> <ul> <li>Observing data stores or repositories</li> <li>Watching database changes (e.g. Room or SQLDelight flows)</li> <li>Listening to real-time updates (e.g. WebSocket streams)</li> <li>Monitoring connectivity or sensor data</li> </ul>"},{"location":"use-cases/overview/","title":"Overview","text":""},{"location":"use-cases/overview/#overview","title":"Overview","text":"<p>Use cases encapsulate business logic and ensure it runs on background threads, keeping your UI layer clean and responsive.</p>"},{"location":"use-cases/overview/#types","title":"Types","text":"<p>Arkitekt provides two use case types in the <code>cr-usecases</code> module:</p> <ul> <li><code>UseCase&lt;ARGS, T&gt;</code>: for one-shot operations that return a single result (e.g. API calls, database writes)</li> <li><code>FlowUseCase&lt;ARGS, T&gt;</code>: for streaming operations that emit multiple values over time (e.g. observing database changes, real-time updates)</li> </ul> <p>Both are interfaces. The <code>cr-usecases</code> module is KMP-compatible and works on both the Android and Decompose/KMP architecture paths.</p>"},{"location":"use-cases/overview/#execution","title":"Execution","text":"<p>Use cases are executed through the <code>CoroutineScopeOwner</code> interface. Its key members are:</p> <ul> <li><code>useCaseScope</code>: the <code>CoroutineScope</code> in which use case coroutines are launched</li> <li><code>useCaseJobPool</code>: a <code>MutableMap&lt;Any, Job&gt;</code> that tracks running jobs for cancellation</li> <li><code>getWorkerDispatcher()</code>: the dispatcher for background work (defaults to <code>Dispatchers.IO</code>)</li> <li><code>launchWithHandler { }</code>: launches a coroutine with structured error handling</li> </ul> <p><code>CoroutineScopeOwner</code> is implemented by:</p> <ul> <li><code>BaseViewModel</code> (Android/Compose path): automatically, using <code>viewModelScope</code></li> <li><code>BaseComponent</code> subclasses (Decompose/KMP path): implement <code>CoroutineScopeOwner</code> manually and set <code>useCaseScope = lifecycleScope</code></li> </ul>"},{"location":"use-cases/overview/#calling-execute","title":"Calling execute","text":"<p>The <code>execute(...)</code> extensions on <code>UseCase</code> and <code>FlowUseCase</code> use Kotlin context parameters. They are called directly from inside any <code>CoroutineScopeOwner</code>:</p> <pre><code>// inside a ViewModel or Component that implements CoroutineScopeOwner\nloginUseCase.execute(args) {\n    onStart { /* ... */ }\n    onSuccess { result -&gt; /* ... */ }\n    onError { error -&gt; /* ... */ }\n}\n</code></pre> <p>The <code>CoroutineScopeOwner</code> context is resolved automatically by the compiler, so no explicit receiver is needed. See UseCase and FlowUseCase for full details.</p>"},{"location":"use-cases/usecase/","title":"UseCase","text":""},{"location":"use-cases/usecase/#usecase","title":"UseCase","text":"<p><code>UseCase&lt;ARGS, T&gt;</code> is an interface for one-shot operations that return a single result.</p>"},{"location":"use-cases/usecase/#defining-a-usecase","title":"Defining a UseCase","text":"<p>Implement the <code>build</code> method with your business logic:</p> <pre><code>class LoginUseCase @Inject constructor(\n    private val authRepository: AuthRepository,\n) : UseCase&lt;LoginData, User&gt; {\n    override suspend fun build(args: LoginData): User =\n        authRepository.login(args.name, args.password)\n}\n</code></pre> <p>For use cases without arguments, use <code>Unit</code> as the args type:</p> <pre><code>class GetCurrentUserUseCase @Inject constructor(\n    private val userRepository: UserRepository,\n) : UseCase&lt;Unit, User&gt; {\n    override suspend fun build(args: Unit): User =\n        userRepository.getCurrentUser()\n}\n</code></pre>"},{"location":"use-cases/usecase/#async-execution-callbacks","title":"Async execution (callbacks)","text":"<p>Execute with callback handlers for fire-and-forget style control flow. Requires a <code>CoroutineScopeOwner</code> in scope (e.g. inside a ViewModel or Component):</p> <pre><code>loginUseCase.execute(LoginData(name, password)) {\n    onStart { isLoading = true }\n    onSuccess { result -&gt;\n        isLoading = false\n        // handle result\n    }\n    onError { error -&gt;\n        isLoading = false\n        // handle error\n    }\n    disposePrevious(true) // default: cancels previous execution\n}\n</code></pre> <p>For <code>Unit</code> args, omit the args parameter:</p> <pre><code>refreshUseCase.execute {\n    onSuccess { /* handle result */ }\n}\n</code></pre>"},{"location":"use-cases/usecase/#callback-options","title":"Callback options","text":"<ul> <li><code>onStart</code>: called before the coroutine starts</li> <li><code>onSuccess(T)</code>: called on successful completion with the result</li> <li><code>onError(Throwable)</code>: called on error; also triggers <code>UseCaseErrorHandler.globalOnErrorLogger</code> (see Error Handling)</li> <li><code>disposePrevious</code>: whether to cancel any previous execution before starting a new one (default <code>true</code>)</li> </ul>"},{"location":"use-cases/usecase/#sync-execution-kotlinresult","title":"Sync execution (kotlin.Result)","text":"<p>Execute and get a <code>kotlin.Result&lt;T&gt;</code> back for sequential coroutine chains. This form is meant to be called from inside <code>launchWithHandler</code>:</p> <pre><code>fun onButtonClicked() = launchWithHandler {\n    val user = loginUseCase\n        .execute(LoginData(name, password))\n        .getOrCancel { error -&gt; showError(error) } // cancels the coroutine on failure\n    // continue with user\n}\n</code></pre> <ul> <li>Returns <code>kotlin.Result&lt;T&gt;</code>: a success wrapping the value or a failure wrapping the exception</li> <li>Accepts an optional <code>cancelPrevious: Boolean</code> parameter (default <code>true</code>)</li> <li><code>CancellationException</code> is rethrown directly; other exceptions are wrapped in <code>Result.failure</code></li> <li>Use <code>getOrCancel { }</code>, <code>getOrThrow()</code>, <code>getOrDefault(...)</code>, <code>getOrNull()</code>, or <code>getOrElse { }</code> to unwrap</li> </ul>"},{"location":"use-cases/usecase/#getorcancel","title":"getOrCancel","text":"<p><code>getOrCancel</code> is a <code>kotlin.Result</code> extension from <code>cr-usecases</code>. On success it returns the value. On failure it calls the optional <code>doBeforeThrow</code> lambda (for side effects like showing an error) and then throws a <code>CancellationException</code>, cleanly cancelling the enclosing coroutine:</p> <pre><code>val data = loadUseCase.execute()\n    .getOrCancel { showErrorMessage(it.message) }\n</code></pre> <p><code>launchWithHandler</code> will route the cancellation through <code>UseCaseErrorHandler.globalOnErrorLogger</code> if the root cause is not itself a <code>CancellationException</code>.</p>"}]}